const LESSONS = [
  {
    day: 1,
    title: "Wealth Is Built, Not Chased",
    theme: "财富不是追出来的",
    idea: "把财富理解成可持续的资产，而不是一时的收入。",
    passage:
      "A good career is not a race toward the next paycheck. It is a process of building assets that can keep working after you stop. Naval often separates wealth from money because money is only a claim on value. Real wealth is a system, a product, a reputation, or a piece of code that produces value repeatedly. When you listen to founders, notice whether they are chasing attention or building something that compounds.",
    zh:
      "今天先把 wealth 和 money 分开。money 是钱，wealth 更像能反复创造价值的资产、系统和声誉。",
    phrases: [
      ["the next paycheck", "下一份薪水；眼前收入"],
      ["a claim on value", "对价值的索取权"],
      ["produces value repeatedly", "反复产生价值"],
      ["building something that compounds", "建设能复利增长的东西"],
      ["chasing attention", "追逐注意力"]
    ],
    sentences: [
      ["A good career is not a race toward the next paycheck.", "好的职业不是奔向下一份薪水的赛跑。", "race toward 连读，paycheck 重读。"],
      ["Real wealth is a system, a product, a reputation, or a piece of code.", "真正的财富是系统、产品、声誉，或一段代码。", "a system / a product / a reputation 三个并列短语要听出节奏。"],
      ["Notice whether they are chasing attention or building something that compounds.", "注意他们是在追逐注意力，还是在建设能复利增长的东西。", "whether they are 常被弱读成 whether they're。"]
    ],
    reflection: "用中文说一遍：wealth 和 money 的区别是什么？"
  },
  {
    day: 2,
    title: "Specific Knowledge",
    theme: "特殊知识",
    idea: "最有价值的能力，往往来自兴趣、经历和长期练习的交叉点。",
    passage:
      "Specific knowledge is hard to teach in a classroom because it is tied to a person's curiosity and taste. You discover it by following what you cannot stop thinking about, then testing it in the real world. It may look strange at first because it does not fit a standard job description. In Silicon Valley, the best founders often turn a personal obsession into a product insight. The market rewards knowledge that is rare, useful, and difficult to copy.",
    zh:
      "specific knowledge 不是普通课堂知识，而是你个人兴趣、判断和经验长期交织出来的能力。",
    phrases: [
      ["tied to curiosity and taste", "和好奇心、品味相连"],
      ["cannot stop thinking about", "忍不住一直想"],
      ["a standard job description", "标准岗位描述"],
      ["a personal obsession", "个人强烈兴趣"],
      ["difficult to copy", "难以复制"]
    ],
    sentences: [
      ["Specific knowledge is hard to teach in a classroom.", "特殊知识很难在课堂里直接教授。", "specific knowledge 是一个整体语块。"],
      ["You discover it by following what you cannot stop thinking about.", "你通过追随自己总是忍不住思考的东西来发现它。", "following what you 连在一起听。"],
      ["The market rewards knowledge that is rare, useful, and difficult to copy.", "市场奖励稀缺、有用、难复制的知识。", "rare, useful, and difficult 三个形容词并列。"]
    ],
    reflection: "你的 specific knowledge 可能在哪几个领域交叉？"
  },
  {
    day: 3,
    title: "Accountability Creates Upside",
    theme: "承担责任带来上行空间",
    idea: "真正的机会常常给愿意署名、承担结果的人。",
    passage:
      "In a large organization, it is easy to hide behind committees, titles, and process. But upside usually belongs to the person who is willing to put a name on the decision. Accountability is uncomfortable because it exposes you to blame, yet it also exposes you to trust. Investors pay attention to founders who can say, clearly and calmly, what they believe and why. A clear owner can move faster than a crowd with no owner.",
    zh:
      "accountability 不是简单的责任，而是愿意公开承担结果；这会带来信任和机会。",
    phrases: [
      ["hide behind committees", "躲在委员会背后"],
      ["put a name on the decision", "为决定署名负责"],
      ["exposes you to blame", "让你暴露在责备中"],
      ["exposes you to trust", "也让你获得信任"],
      ["a clear owner", "明确的负责人"]
    ],
    sentences: [
      ["Upside usually belongs to the person who is willing to put a name on the decision.", "上行收益通常属于愿意为决定署名的人。", "belongs to the person 连读。"],
      ["Accountability is uncomfortable because it exposes you to blame.", "承担责任让人不舒服，因为它让你可能被责备。", "because it 常弱读。"],
      ["A clear owner can move faster than a crowd with no owner.", "一个明确负责人，比一群没人负责的人行动更快。", "clear owner 中 owner 要听清。"]
    ],
    reflection: "为什么承担责任同时意味着风险和机会？"
  },
  {
    day: 4,
    title: "Leverage Changes the Game",
    theme: "杠杆改变游戏",
    idea: "现代杠杆让个人也能放大判断力和创造力。",
    passage:
      "Leverage means using tools that multiply your effort. Capital, labor, code, and media are different forms of leverage, but they do not behave the same way. Code and media are permissionless: you can start with a laptop, publish globally, and improve while the world watches. That is why a tiny team can sometimes create more value than a large department. The key is not to work harder forever; it is to attach your judgment to a scalable machine.",
    zh:
      "leverage 是放大器。代码和媒体尤其重要，因为普通人也可以从很小开始。",
    phrases: [
      ["multiply your effort", "放大你的努力"],
      ["different forms of leverage", "不同形式的杠杆"],
      ["permissionless", "无需许可的"],
      ["publish globally", "面向全球发布"],
      ["a scalable machine", "可扩展的机器/系统"]
    ],
    sentences: [
      ["Leverage means using tools that multiply your effort.", "杠杆就是使用能放大努力的工具。", "using tools that 可连读。"],
      ["Code and media are permissionless.", "代码和媒体是无需许可的杠杆。", "permissionless 是本课关键词。"],
      ["Attach your judgment to a scalable machine.", "把你的判断力接到可扩展的机器上。", "attach your 中 ch 与 your 相接。"]
    ],
    reflection: "在今天，一个普通人有哪些 permissionless leverage？"
  },
  {
    day: 5,
    title: "Productize Yourself",
    theme: "把自己产品化",
    idea: "做自己擅长又真正在意的事，再让技术和媒体扩大它。",
    passage:
      "To productize yourself, you need two ingredients: authenticity and leverage. Authenticity means you are not copying someone else's game. Leverage means the work can travel beyond your own hours. A writer, investor, teacher, or founder can all become more effective when their best thinking is packaged into a repeatable product. The difficult part is not the slogan; it is choosing a narrow area where your energy stays high for years.",
    zh:
      "productize yourself 不是把人变商品，而是把你的独特能力变成可传播、可重复创造价值的产品。",
    phrases: [
      ["two ingredients", "两个要素"],
      ["someone else's game", "别人的游戏"],
      ["travel beyond your own hours", "超越你个人时间继续传播"],
      ["packaged into a repeatable product", "包装成可重复的产品"],
      ["a narrow area", "一个窄领域"]
    ],
    sentences: [
      ["Authenticity means you are not copying someone else's game.", "真实意味着你不是在复制别人的游戏。", "someone else's game 连着听。"],
      ["Leverage means the work can travel beyond your own hours.", "杠杆意味着工作能超越你的个人时间。", "beyond your own hours 是核心语块。"],
      ["Choose a narrow area where your energy stays high for years.", "选择一个你能多年保持能量的窄领域。", "where your energy 可弱读。"]
    ],
    reflection: "你有哪些能力可以被 productize？"
  },
  {
    day: 6,
    title: "Play Long-Term Games",
    theme: "玩长期游戏",
    idea: "长期关系、长期声誉和长期复利，胜过短期技巧。",
    passage:
      "Short-term games reward clever moves. Long-term games reward trust, patience, and consistency. In venture capital, the best deals often come through relationships that were built before anyone needed a deal. In life, the same pattern appears again and again: people prefer to work with those whose behavior is predictable and generous. If you want compounding, you have to stay in the game long enough for compounding to matter.",
    zh:
      "长期游戏的回报不是一次性赢，而是信任、声誉和关系不断复利。",
    phrases: [
      ["clever moves", "聪明招数"],
      ["built before anyone needed a deal", "在需要交易前就建立起来"],
      ["predictable and generous", "稳定可靠且慷慨"],
      ["stay in the game", "留在游戏里"],
      ["for compounding to matter", "让复利真正起作用"]
    ],
    sentences: [
      ["Short-term games reward clever moves.", "短期游戏奖励聪明招数。", "short-term 与 long-term 对比听。"],
      ["The best deals often come through relationships.", "最好的交易常常来自关系。", "come through 表示通过某种渠道出现。"],
      ["Stay in the game long enough for compounding to matter.", "在游戏里待得足够久，让复利变得重要。", "long enough for 是常见语块。"]
    ],
    reflection: "你现在最值得长期投入的关系或能力是什么？"
  },
  {
    day: 7,
    title: "Judgment Is the Highest Leverage",
    theme: "判断力是最高杠杆",
    idea: "当工具越来越强，决定做什么比努力本身更值钱。",
    passage:
      "As tools become more powerful, judgment becomes more valuable. A small decision can direct a large amount of capital, code, or attention. That is why great investors spend so much time reading, thinking, and waiting. They are not trying to be busy every hour; they are trying to be right when it matters. Judgment improves when you remove noise, study reality, and admit what you do not know.",
    zh:
      "工具越强，判断力越值钱。因为一个小判断可能调动巨大的资源。",
    phrases: [
      ["a large amount of capital", "大量资本"],
      ["trying to be right when it matters", "在关键时刻判断正确"],
      ["remove noise", "去除噪音"],
      ["study reality", "研究现实"],
      ["admit what you do not know", "承认自己不知道的事"]
    ],
    sentences: [
      ["As tools become more powerful, judgment becomes more valuable.", "工具越强，判断力越有价值。", "as 表示随着。"],
      ["They are not trying to be busy every hour.", "他们不是试图每个小时都很忙。", "trying to 常弱读成 tryin' to。"],
      ["Judgment improves when you remove noise and study reality.", "当你去除噪音、研究现实时，判断力会提高。", "remove noise 是高频表达。"]
    ],
    reflection: "你最近一个重要判断，噪音在哪里？"
  },
  {
    day: 8,
    title: "Reading Is a Force Multiplier",
    theme: "阅读是倍增器",
    idea: "读书不是为了显得博学，而是为了提高判断质量。",
    passage:
      "Reading is not a status signal; it is a way to borrow other people's experiments. A good book can compress ten years of mistakes into a few quiet hours. Naval prefers ideas that survive time because they are less likely to be fashion. The goal is not to finish more books than your friends. The goal is to find a few mental models that change how you see the world.",
    zh:
      "阅读是借用别人实验结果的方式，重点不是数量，而是找到能改变判断的模型。",
    phrases: [
      ["a status signal", "地位信号"],
      ["borrow other people's experiments", "借用别人的实验"],
      ["compress ten years of mistakes", "压缩十年的错误经验"],
      ["survive time", "经受时间考验"],
      ["mental models", "思维模型"]
    ],
    sentences: [
      ["Reading is not a status signal.", "阅读不是地位信号。", "status signal 是思想视频常见语块。"],
      ["A good book can compress ten years of mistakes.", "一本好书能压缩十年的错误经验。", "compress ten years 注意重音。"],
      ["Find a few mental models that change how you see the world.", "找到几个改变你看世界方式的思维模型。", "how you see the world 连着听。"]
    ],
    reflection: "哪一本书曾经改变过你的判断？"
  },
  {
    day: 9,
    title: "Escape Competition Through Authenticity",
    theme: "用真实逃离竞争",
    idea: "当你做最像自己的事，竞争会变少。",
    passage:
      "Competition is useful in sports, but it can be a trap in careers. If you copy the same goals, credentials, and signals as everyone else, you enter a crowded market. Authenticity is not a soft word here; it is a strategy. No one can compete with you at being you, if you are willing to take the risk of looking different. The more your work reflects your own curiosity, the harder it is to commoditize.",
    zh:
      "这里的 authenticity 不是鸡汤，而是战略：做真正适合自己的事，竞争会少很多。",
    phrases: [
      ["a trap in careers", "职业中的陷阱"],
      ["the same goals and signals", "同样的目标和信号"],
      ["a crowded market", "拥挤市场"],
      ["the risk of looking different", "看起来不同的风险"],
      ["harder to commoditize", "更难被商品化"]
    ],
    sentences: [
      ["If you copy the same signals as everyone else, you enter a crowded market.", "如果你复制所有人的信号，你就进入拥挤市场。", "as everyone else 常连读。"],
      ["Authenticity is not a soft word here; it is a strategy.", "真实在这里不是软词，而是战略。", "not A; it is B 的转折节奏。"],
      ["No one can compete with you at being you.", "没人能在做你自己这件事上和你竞争。", "compete with you at 是固定搭配。"]
    ],
    reflection: "你的职业或学习中，哪里正在复制别人的信号？"
  },
  {
    day: 10,
    title: "Desire Is a Contract",
    theme: "欲望是一份合同",
    idea: "你想要什么，也会被什么束缚。",
    passage:
      "Every desire looks like freedom from a distance. Up close, it often becomes a contract: until you get what you want, part of your mind is not free. This does not mean desire is bad. It means you should choose your desires carefully, the way an investor chooses a portfolio. A good desire points you toward growth; a bad desire keeps you comparing yourself with people you do not even admire.",
    zh:
      "欲望不是坏事，但每个欲望都会占用心智。要像选投资组合一样选择欲望。",
    phrases: [
      ["from a distance", "从远处看"],
      ["up close", "近距离看"],
      ["part of your mind is not free", "你的部分心智并不自由"],
      ["choose your desires carefully", "谨慎选择欲望"],
      ["people you do not even admire", "你其实并不敬佩的人"]
    ],
    sentences: [
      ["Every desire looks like freedom from a distance.", "每个欲望从远处看都像自由。", "from a distance 是常用尾部语块。"],
      ["Until you get what you want, part of your mind is not free.", "在得到想要的东西之前，你的部分心智并不自由。", "until you get 连着听。"],
      ["A good desire points you toward growth.", "好的欲望指向成长。", "points you toward 注意 toward 的弱读。"]
    ],
    reflection: "你现在最值得保留的欲望是什么？最该放下的是什么？"
  },
  {
    day: 11,
    title: "Status Games Are Expensive",
    theme: "地位游戏很昂贵",
    idea: "为了被别人看见而生活，会消耗大量注意力。",
    passage:
      "Status games are everywhere because humans constantly measure themselves against other humans. The problem is that status is relative: your win often requires someone else's loss. Wealth creation is different because it can expand the pie. When a founder builds a useful product, customers can win, employees can win, and investors can win. A quieter life begins when you notice which games are positive-sum and which games are only about ranking.",
    zh:
      "status game 是比较排名；wealth creation 是创造价值。两者的心理结构很不一样。",
    phrases: [
      ["measure themselves against others", "拿自己和别人比较"],
      ["status is relative", "地位是相对的"],
      ["expand the pie", "把蛋糕做大"],
      ["positive-sum", "正和的"],
      ["only about ranking", "只是关于排名"]
    ],
    sentences: [
      ["Status is relative: your win often requires someone else's loss.", "地位是相对的：你的赢常常需要别人输。", "relative 后面的解释要听成一整块。"],
      ["Wealth creation is different because it can expand the pie.", "财富创造不同，因为它能把蛋糕做大。", "expand the pie 是商业英语常见比喻。"],
      ["Notice which games are positive-sum.", "注意哪些游戏是正和的。", "which games are 可弱读。"]
    ],
    reflection: "你生活中哪些事情是 status game？"
  },
  {
    day: 12,
    title: "Peace Is a Competitive Advantage",
    theme: "平静是一种竞争优势",
    idea: "一个平静的人，更容易看清现实、做长期决定。",
    passage:
      "A calm mind is not passive. It is a mind that can see clearly without being dragged around by every notification, headline, and opinion. In markets, panic creates bad trades. In startups, panic creates bad hiring and bad product decisions. The person who can pause, observe, and think from first principles has an advantage that does not look dramatic from the outside.",
    zh:
      "平静不是消极，而是不被通知、标题和舆论拖着走。这对投资和创业都重要。",
    phrases: [
      ["a calm mind", "平静的心智"],
      ["dragged around by every notification", "被每条通知拖着走"],
      ["panic creates bad trades", "恐慌制造糟糕交易"],
      ["think from first principles", "从第一性原理思考"],
      ["does not look dramatic", "看起来不戏剧化"]
    ],
    sentences: [
      ["A calm mind is not passive.", "平静的心智并不是消极的。", "calm 中 l 不发音。"],
      ["Panic creates bad trades.", "恐慌制造糟糕交易。", "短句，适合模仿重音。"],
      ["The person who can pause and observe has an advantage.", "能暂停并观察的人有优势。", "pause and observe 连读。"]
    ],
    reflection: "你通常在什么情况下最容易被 headline 带走？"
  },
  {
    day: 13,
    title: "Happiness Is a Skill",
    theme: "幸福是一种技能",
    idea: "幸福不是奖励，而是可以训练的内部能力。",
    passage:
      "Happiness is often treated as a prize that arrives after success. Naval's view is closer to training: happiness is a skill you practice by noticing your thoughts, reducing unnecessary desires, and returning to the present. This sounds simple, but it is not easy because the mind loves to rehearse the past and simulate the future. A person can be wealthy and restless, or modest and deeply at ease. The difference is not always external.",
    zh:
      "幸福不是成功之后才发放的奖品，而是一种可以训练的能力。",
    phrases: [
      ["treated as a prize", "被当作奖品"],
      ["noticing your thoughts", "觉察自己的念头"],
      ["unnecessary desires", "不必要的欲望"],
      ["rehearse the past", "反复排练过去"],
      ["deeply at ease", "深深安定"]
    ],
    sentences: [
      ["Happiness is often treated as a prize that arrives after success.", "幸福常被当作成功之后才来的奖品。", "treated as a prize 是核心语块。"],
      ["The mind loves to rehearse the past and simulate the future.", "头脑喜欢反复排练过去、模拟未来。", "rehearse 和 simulate 是思想类视频高频词。"],
      ["The difference is not always external.", "区别不总是在外部。", "external 重音在第二音节。"]
    ],
    reflection: "如果幸福是一种技能，你今天可以练哪一小步？"
  },
  {
    day: 14,
    title: "First Principles",
    theme: "第一性原理",
    idea: "把问题拆到不能再拆，避免只跟着惯例走。",
    passage:
      "First-principles thinking begins when you stop accepting the default explanation. Instead of asking what people usually do, you ask what must be true. A founder may use this to rethink a market; an investor may use it to avoid a popular mistake. The method is uncomfortable because it removes borrowed certainty. But it also lets you see possibilities that are hidden behind convention.",
    zh:
      "第一性原理就是不只问别人通常怎么做，而是问什么必须为真。",
    phrases: [
      ["the default explanation", "默认解释"],
      ["what must be true", "什么必须为真"],
      ["rethink a market", "重新思考一个市场"],
      ["borrowed certainty", "借来的确定感"],
      ["hidden behind convention", "藏在惯例背后"]
    ],
    sentences: [
      ["First-principles thinking begins when you stop accepting the default explanation.", "第一性原理思考始于你停止接受默认解释。", "default explanation 注意连读。"],
      ["Instead of asking what people usually do, you ask what must be true.", "你不问人们通常怎么做，而问什么必须为真。", "instead of asking 是常见开头。"],
      ["It removes borrowed certainty.", "它拿掉借来的确定感。", "borrowed certainty 是漂亮语块。"]
    ],
    reflection: "你最近在哪件事上接受了 default explanation？"
  },
  {
    day: 15,
    title: "Compound Interest in Life",
    theme: "人生里的复利",
    idea: "复利不只在金钱里，也在习惯、关系、知识和声誉里。",
    passage:
      "People understand compound interest in finance, but they underestimate it in ordinary life. A daily habit, a trusted relationship, a clear writing practice, or a reputation for fairness can all compound. The early days feel slow because the curve is still flat. Many people quit before the curve has a chance to bend upward. Long-term thinkers learn to respect boring consistency.",
    zh:
      "复利刚开始很慢，所以很多人还没等曲线上扬就放弃了。",
    phrases: [
      ["underestimate it in ordinary life", "低估它在日常生活里的作用"],
      ["a reputation for fairness", "公平可靠的声誉"],
      ["the curve is still flat", "曲线仍然平坦"],
      ["bend upward", "向上弯曲"],
      ["boring consistency", "无聊但稳定的一致性"]
    ],
    sentences: [
      ["They underestimate compound interest in ordinary life.", "他们低估复利在日常生活中的作用。", "underestimate 较长，重音在 es。"],
      ["Many people quit before the curve has a chance to bend upward.", "很多人在曲线有机会向上弯之前就放弃了。", "before the curve has a chance 是整块。"],
      ["Long-term thinkers learn to respect boring consistency.", "长期主义者学会尊重无聊但稳定的一致性。", "boring consistency 是可收藏表达。"]
    ],
    reflection: "你愿意让哪一个习惯复利十年？"
  },
  {
    day: 16,
    title: "Earn With Your Mind",
    theme: "用头脑赚钱",
    idea: "最高级的收入来自判断力、创造力和可复制的产出。",
    passage:
      "There is nothing wrong with working hard, but labor alone has a ceiling. If your income depends only on hours, your growth is limited by the calendar. Earning with your mind means using judgment, creativity, and leverage to create outcomes that are not tied one-to-one to time. This is why software, media, investing, and ownership matter so much. They allow ideas to move faster than a person's body.",
    zh:
      "用时间换钱有天花板；用判断、创造和杠杆创造价值，才有更大空间。",
    phrases: [
      ["labor alone has a ceiling", "单靠劳动有天花板"],
      ["limited by the calendar", "受日历限制"],
      ["not tied one-to-one to time", "不和时间一比一绑定"],
      ["ownership matters", "所有权很重要"],
      ["move faster than a person's body", "比人的身体移动更快"]
    ],
    sentences: [
      ["If your income depends only on hours, your growth is limited by the calendar.", "如果收入只取决于小时数，增长就被日历限制。", "depends only on hours 连读。"],
      ["Earning with your mind means using judgment, creativity, and leverage.", "用头脑赚钱意味着使用判断力、创造力和杠杆。", "三个名词并列。"],
      ["Ideas can move faster than a person's body.", "思想可以比人的身体移动得更快。", "faster than a 常连读。"]
    ],
    reflection: "你的收入或价值创造，哪些部分还在和时间一比一绑定？"
  },
  {
    day: 17,
    title: "Ownership Matters",
    theme: "所有权很重要",
    idea: "工资提供稳定，所有权提供非线性上行。",
    passage:
      "Salary can give you stability, but ownership gives you exposure to upside. This is why founders and early employees care so much about equity. Of course, ownership also carries risk; most startups do not become breakout companies. But without ownership, you may contribute to a growing machine while capturing only a fixed slice. Naval's point is not to reject salary, but to understand the difference between renting your time and owning a piece of the outcome.",
    zh:
      "equity 和 ownership 是硅谷视频里极常见的词。它们意味着你参与结果，而不只是出售时间。",
    phrases: [
      ["exposure to upside", "接触上行收益"],
      ["early employees", "早期员工"],
      ["breakout companies", "爆发式成功的公司"],
      ["a fixed slice", "固定的一小块"],
      ["owning a piece of the outcome", "拥有结果的一部分"]
    ],
    sentences: [
      ["Salary can give you stability, but ownership gives you exposure to upside.", "工资给你稳定，所有权给你上行空间。", "but 后面的 ownership 要重读。"],
      ["Most startups do not become breakout companies.", "大多数初创公司不会成为爆发式成功的公司。", "breakout companies 是创业常用语。"],
      ["Understand the difference between renting your time and owning a piece of the outcome.", "理解出租时间和拥有结果一部分的区别。", "between A and B 结构。"]
    ],
    reflection: "你怎样理解 salary 和 ownership 的取舍？"
  },
  {
    day: 18,
    title: "Reputation Is a Ledger",
    theme: "声誉是一种账本",
    idea: "你每次选择都在写入别人心里的账本。",
    passage:
      "Reputation is a ledger that other people keep for you. You cannot edit it directly; you can only add new entries through behavior. In small communities like startups and investing, this ledger travels faster than formal resumes. People remember who was honest when there was pressure, who shared credit, and who disappeared when things became difficult. A good reputation lowers transaction costs because people do not need to inspect every detail before trusting you.",
    zh:
      "声誉像别人替你保存的账本。你不能直接修改，只能用行为持续写入。",
    phrases: [
      ["a ledger", "账本"],
      ["add new entries through behavior", "通过行为写入新记录"],
      ["formal resumes", "正式简历"],
      ["shared credit", "分享功劳"],
      ["lowers transaction costs", "降低交易成本"]
    ],
    sentences: [
      ["Reputation is a ledger that other people keep for you.", "声誉是别人替你保存的账本。", "ledger 是投资/商业语境常见词。"],
      ["You can only add new entries through behavior.", "你只能通过行为写入新记录。", "add new entries 可整块记。"],
      ["A good reputation lowers transaction costs.", "好的声誉降低交易成本。", "lowers transaction costs 是高价值表达。"]
    ],
    reflection: "你希望别人心里的那本 ledger 记录你什么？"
  },
  {
    day: 19,
    title: "Make Luck Find You",
    theme: "让运气找到你",
    idea: "行动、作品和公开表达，会增加好运接触你的机会。",
    passage:
      "Some luck is blind, but some luck is attracted by motion. When you write, build, invest, introduce people, or publish useful thoughts, you create more surface area for luck. A quiet expert who never shares anything may be excellent, but the world has fewer ways to discover him. In Silicon Valley, opportunity often appears as a weak signal before it becomes obvious. The more prepared and visible you are, the more likely you are to notice it.",
    zh:
      "surface area for luck 很重要：你创造的作品和连接越多，运气越容易碰到你。",
    phrases: [
      ["attracted by motion", "被行动吸引"],
      ["surface area for luck", "好运接触你的表面积"],
      ["a quiet expert", "沉默的专家"],
      ["a weak signal", "弱信号"],
      ["prepared and visible", "有准备且可被看见"]
    ],
    sentences: [
      ["Some luck is blind, but some luck is attracted by motion.", "有些运气是盲目的，但有些运气会被行动吸引。", "blind 和 attracted 形成对比。"],
      ["You create more surface area for luck.", "你为好运创造更多接触面积。", "surface area 是非常好用的语块。"],
      ["Opportunity often appears as a weak signal before it becomes obvious.", "机会在变得明显前，常以弱信号出现。", "before it becomes obvious 连读。"]
    ],
    reflection: "你可以怎样增加自己的 surface area for luck？"
  },
  {
    day: 20,
    title: "The Internet Rewards Niche Taste",
    theme: "互联网奖励小众品味",
    idea: "全球市场让小众兴趣也可能找到足够多的同路人。",
    passage:
      "Before the internet, many people had to hide their unusual interests because local markets were too small. Online, niche taste can become an advantage. A founder can serve a tiny group extremely well, then expand from there. A teacher can speak to a narrow audience and still reach thousands of serious learners. The internet rewards people who are willing to be precise about what they love and who they serve.",
    zh:
      "互联网让小众不再等于小市场。越精确，越可能找到真正需要你的人。",
    phrases: [
      ["unusual interests", "不寻常的兴趣"],
      ["local markets were too small", "本地市场太小"],
      ["serve a tiny group extremely well", "极好地服务一个小群体"],
      ["a narrow audience", "窄受众"],
      ["be precise about what they love", "精确说出自己热爱的东西"]
    ],
    sentences: [
      ["Online, niche taste can become an advantage.", "在线世界里，小众品味可以变成优势。", "niche 可读 /neesh/ 或 /nitch/。"],
      ["A founder can serve a tiny group extremely well.", "创始人可以极好地服务一个小群体。", "serve a tiny group 连读。"],
      ["The internet rewards people who are willing to be precise.", "互联网奖励愿意精确表达的人。", "willing to be 常弱读。"]
    ],
    reflection: "你有哪些 niche taste 可以成为学习入口？"
  },
  {
    day: 21,
    title: "AI as Permissionless Leverage",
    theme: "AI 是新的无需许可杠杆",
    idea: "AI 放大表达、研究和创造，但仍需要人的判断力。",
    passage:
      "AI has made leverage even more accessible. A single person can draft, research, translate, code, and test ideas faster than a small team could a decade ago. But better tools do not remove the need for judgment; they increase the cost of poor judgment. If everyone can produce more, the scarce skill becomes choosing what is worth producing. Naval's older idea about permissionless leverage becomes even more important in an AI world.",
    zh:
      "AI 让杠杆更便宜，但判断力更重要。因为人人都能生成内容，选择做什么才稀缺。",
    phrases: [
      ["more accessible", "更容易获得"],
      ["a decade ago", "十年前"],
      ["remove the need for judgment", "取消对判断力的需要"],
      ["the scarce skill", "稀缺能力"],
      ["worth producing", "值得生产/创造"]
    ],
    sentences: [
      ["AI has made leverage even more accessible.", "AI 让杠杆变得更容易获得。", "has made 是现在完成时，听成整体。"],
      ["Better tools do not remove the need for judgment.", "更好的工具不会取消对判断力的需要。", "remove the need for 是重要表达。"],
      ["The scarce skill becomes choosing what is worth producing.", "稀缺能力变成了选择什么值得创造。", "what is worth producing 连读。"]
    ],
    reflection: "在 AI 时代，你最想用 AI 放大哪种判断或能力？"
  },
  {
    day: 22,
    title: "Learn to Sell, Learn to Build",
    theme: "学会销售，学会建造",
    idea: "会建造和会销售，是创业世界最强的两种能力。",
    passage:
      "Naval often says that if you can build and you can sell, you become dangerous in the best sense. Building turns ideas into reality. Selling transfers belief from one mind to another. Many technical people avoid selling because they confuse it with manipulation, but honest selling is really clear communication about value. A founder who can both build the product and explain why it matters has unusual range.",
    zh:
      "build 是把想法变成现实；sell 是把价值讲清楚。两者合在一起很强。",
    phrases: [
      ["dangerous in the best sense", "褒义上的厉害"],
      ["turns ideas into reality", "把想法变成现实"],
      ["transfers belief", "传递信念"],
      ["confuse it with manipulation", "把它和操纵混淆"],
      ["unusual range", "少见的能力跨度"]
    ],
    sentences: [
      ["If you can build and you can sell, you become dangerous in the best sense.", "如果你既会建造又会销售，你会变得很厉害。", "in the best sense 是缓和语气。"],
      ["Selling transfers belief from one mind to another.", "销售把信念从一个头脑传到另一个头脑。", "transfers belief 是核心短语。"],
      ["Honest selling is clear communication about value.", "诚实销售就是把价值讲清楚。", "clear communication about value 可收藏。"]
    ],
    reflection: "你更擅长 build 还是 sell？另一个能力怎么练？"
  },
  {
    day: 23,
    title: "The Best Work Feels Like Play",
    theme: "最好的工作像游戏",
    idea: "你觉得像玩的事，别人可能觉得很辛苦，这就是优势。",
    passage:
      "The best work often feels like play to the person doing it, even when it looks like work to everyone else. This is not about avoiding effort. It is about finding a field where your natural curiosity supplies energy that other people have to force. A founder obsessed with a problem can keep learning after competitors are tired. When effort and curiosity point in the same direction, endurance becomes easier.",
    zh:
      "这不是逃避努力，而是找到一个你天然有能量、别人却觉得累的领域。",
    phrases: [
      ["feels like play", "感觉像玩"],
      ["avoiding effort", "逃避努力"],
      ["natural curiosity supplies energy", "天然好奇心提供能量"],
      ["obsessed with a problem", "对一个问题着迷"],
      ["endurance becomes easier", "耐力变得更容易"]
    ],
    sentences: [
      ["The best work often feels like play to the person doing it.", "最好的工作对做它的人来说常常像游戏。", "to the person doing it 是插入说明。"],
      ["Natural curiosity supplies energy.", "天然好奇心提供能量。", "supplies energy 是好表达。"],
      ["When effort and curiosity point in the same direction, endurance becomes easier.", "当努力和好奇心指向同一方向，耐力会更容易。", "point in the same direction 是核心语块。"]
    ],
    reflection: "什么事对你像 play，但对别人像 work？"
  },
  {
    day: 24,
    title: "Avoid Ruin",
    theme: "避免毁灭性风险",
    idea: "高收益没有意义，如果一次错误就让你出局。",
    passage:
      "Smart risk-taking is not the same as reckless risk-taking. Investors understand that the first rule is to avoid ruin. If one decision can destroy your health, reputation, or ability to keep playing, the expected return may not matter. Good risk has limited downside and meaningful upside. Bad risk looks exciting because it hides the cost until it is too late.",
    zh:
      "risk 不是越大越好。真正聪明的风险，是下行有限、上行有意义。",
    phrases: [
      ["reckless risk-taking", "鲁莽冒险"],
      ["avoid ruin", "避免毁灭性失败"],
      ["keep playing", "继续留在游戏中"],
      ["limited downside", "有限下行"],
      ["meaningful upside", "有意义的上行"]
    ],
    sentences: [
      ["Smart risk-taking is not the same as reckless risk-taking.", "聪明冒险不等于鲁莽冒险。", "not the same as 是常用对比。"],
      ["The first rule is to avoid ruin.", "第一条规则是避免毁灭性失败。", "avoid ruin 是投资常用表达。"],
      ["Good risk has limited downside and meaningful upside.", "好风险有有限下行和有意义上行。", "downside / upside 成对记。"]
    ],
    reflection: "你现在面对的一个风险，是 good risk 还是 bad risk？"
  },
  {
    day: 25,
    title: "Optionality",
    theme: "选择权",
    idea: "保留选择权，让你在不确定世界里更灵活。",
    passage:
      "Optionality is the value of having more than one good path. In a fast-changing world, it can be more valuable than a detailed five-year plan. Skills, savings, relationships, and a strong reputation all create optionality. But optionality can become an excuse for never committing. The art is to keep enough freedom to adapt while still choosing a few long-term games seriously.",
    zh:
      "optionality 是选择权。它很重要，但也不能变成永远不投入的借口。",
    phrases: [
      ["more than one good path", "不止一条好路"],
      ["a fast-changing world", "快速变化的世界"],
      ["create optionality", "创造选择权"],
      ["an excuse for never committing", "永不投入的借口"],
      ["freedom to adapt", "适应变化的自由"]
    ],
    sentences: [
      ["Optionality is the value of having more than one good path.", "选择权是不止一条好路的价值。", "optionality 重音在 nal。"],
      ["It can be more valuable than a detailed five-year plan.", "它可能比详细的五年计划更有价值。", "more valuable than 连读。"],
      ["Keep enough freedom to adapt while still choosing a few long-term games.", "保留足够适应自由，同时认真选择几个长期游戏。", "while still 表示同时仍然。"]
    ],
    reflection: "你的技能、储蓄和关系，正在给你创造 optionality 吗？"
  },
  {
    day: 26,
    title: "Attention Is the Real Currency",
    theme: "注意力是真正的货币",
    idea: "你把注意力投向哪里，人生就慢慢变成哪里。",
    passage:
      "Attention is the currency that every app, platform, and media company wants from you. If you spend it unconsciously, other people will allocate your life for you. Naval's advice often points back to sovereignty over the mind: choose what enters your attention, and choose what deserves repetition. A person who protects attention can think deeply. A person who cannot protect attention becomes easy to steer.",
    zh:
      "注意力是所有平台都想争夺的货币。保护注意力，就是保护你的判断力和人生方向。",
    phrases: [
      ["spend it unconsciously", "无意识地花掉它"],
      ["allocate your life for you", "替你分配人生"],
      ["sovereignty over the mind", "心智主权"],
      ["deserves repetition", "值得重复"],
      ["easy to steer", "容易被操控方向"]
    ],
    sentences: [
      ["Attention is the currency that every app wants from you.", "注意力是每个 App 都想从你这里得到的货币。", "that every app wants from you 是定语从句。"],
      ["If you spend it unconsciously, other people will allocate your life for you.", "如果你无意识花掉它，别人就会替你分配人生。", "allocate your life 是强表达。"],
      ["A person who protects attention can think deeply.", "能保护注意力的人才能深入思考。", "protects attention 连着听。"]
    ],
    reflection: "今天什么东西最容易拿走你的 attention？"
  },
  {
    day: 27,
    title: "Clear Writing Reveals Clear Thinking",
    theme: "清晰写作暴露清晰思考",
    idea: "写不清，往往是想不清。",
    passage:
      "Clear writing is not decoration. It is evidence of clear thinking. When a founder explains a product in simple language, investors can see whether the idea is sharp or vague. Jargon often hides confusion, especially in technology markets where everyone wants to sound advanced. If you cannot explain the value without fog, you may not understand it yet. Writing forces vague thoughts to become testable.",
    zh:
      "写作不是装饰。它逼迫模糊想法变得可检查、可测试。",
    phrases: [
      ["not decoration", "不是装饰"],
      ["evidence of clear thinking", "清晰思考的证据"],
      ["sharp or vague", "锐利还是模糊"],
      ["hides confusion", "隐藏混乱"],
      ["without fog", "没有雾气般的含糊"]
    ],
    sentences: [
      ["Clear writing is evidence of clear thinking.", "清晰写作是清晰思考的证据。", "clear writing / clear thinking 对称。"],
      ["Jargon often hides confusion.", "行话常常隐藏混乱。", "jargon 是商业科技视频常见词。"],
      ["Writing forces vague thoughts to become testable.", "写作迫使模糊想法变得可测试。", "forces vague thoughts to 连读。"]
    ],
    reflection: "你能用一句简单英文解释自己最近关心的一个想法吗？"
  },
  {
    day: 28,
    title: "The Market Is a Teacher",
    theme: "市场是一位老师",
    idea: "市场反馈很冷，但它能告诉你别人是否真正需要。",
    passage:
      "The market is not always wise, but it is always giving feedback. A product that people praise but never use is sending a different signal from a product that people quietly pay for every month. Founders learn to respect revealed preference: what people actually do, not what they say in a survey. This can feel harsh because it ignores your intentions. But it also helps you leave fantasy and return to reality.",
    zh:
      "revealed preference 是非常重要的表达：看人们实际做什么，而不是只听他们说什么。",
    phrases: [
      ["giving feedback", "给出反馈"],
      ["quietly pay for every month", "每月默默付费"],
      ["revealed preference", "显示性偏好；实际选择"],
      ["what people actually do", "人们实际做什么"],
      ["leave fantasy and return to reality", "离开幻想，回到现实"]
    ],
    sentences: [
      ["The market is not always wise, but it is always giving feedback.", "市场不总是明智的，但它总是在给反馈。", "not always wise, but always giving feedback 对比听。"],
      ["Respect revealed preference.", "尊重显示性偏好。", "短句，适合反复听。"],
      ["It helps you leave fantasy and return to reality.", "它帮你离开幻想，回到现实。", "leave fantasy and return to reality 节奏清楚。"]
    ],
    reflection: "你最近有没有只听 praise，却没看 revealed preference？"
  },
  {
    day: 29,
    title: "Choose Your Peers Carefully",
    theme: "谨慎选择同伴",
    idea: "身边人会改变你的标准、欲望和节奏。",
    passage:
      "Your peer group quietly shapes your ambitions. If everyone around you is playing status games, status will feel normal. If everyone around you is building, reading, investing, and telling the truth, your standards rise without a speech. This is one reason Silicon Valley has been powerful: dense networks can transmit ambition quickly. But you can also build a peer group online by choosing whose ideas you let into your mind every day.",
    zh:
      "peer group 会悄悄塑造你的目标和标准。线上关注谁，也是一种选择同伴。",
    phrases: [
      ["quietly shapes your ambitions", "悄悄塑造你的抱负"],
      ["status will feel normal", "地位游戏会显得正常"],
      ["your standards rise", "你的标准提高"],
      ["dense networks", "高密度网络"],
      ["transmit ambition", "传递抱负"]
    ],
    sentences: [
      ["Your peer group quietly shapes your ambitions.", "你的同伴群体悄悄塑造你的抱负。", "peer group 是常用表达。"],
      ["Your standards rise without a speech.", "不用演讲，你的标准也会提高。", "without a speech 很口语。"],
      ["Choose whose ideas you let into your mind every day.", "选择你每天让谁的思想进入你的头脑。", "whose ideas you let into your mind 是长语块。"]
    ],
    reflection: "你每天让哪些人的 ideas 进入自己的 mind？"
  },
  {
    day: 30,
    title: "A Life of Compounding",
    theme: "复利式人生",
    idea: "把判断力、健康、关系、知识和作品放进同一个长期系统。",
    passage:
      "A compounding life is not built from one dramatic decision. It is built from a small number of choices repeated with care: protect your health, choose long-term games, build specific knowledge, use permissionless leverage, and avoid status traps. The result may look slow for a long time, then suddenly obvious. Naval's strongest ideas are connected by one question: what can you do today that your future self will thank you for repeating?",
    zh:
      "最后一天做整合：健康、知识、关系、作品、判断力，都可以进入复利系统。",
    phrases: [
      ["a compounding life", "复利式人生"],
      ["one dramatic decision", "一个戏剧性决定"],
      ["repeated with care", "认真地重复"],
      ["status traps", "地位陷阱"],
      ["your future self", "未来的自己"]
    ],
    sentences: [
      ["A compounding life is not built from one dramatic decision.", "复利式人生不是由一个戏剧性决定建成的。", "built from one dramatic decision 是整块。"],
      ["The result may look slow for a long time, then suddenly obvious.", "结果可能很长时间看起来很慢，然后突然变得明显。", "slow for a long time, then suddenly obvious 节奏重要。"],
      ["What can you do today that your future self will thank you for repeating?", "你今天能做什么，让未来的自己感谢你反复做？", "future self 是思想类视频常见表达。"]
    ],
    reflection: "30 天之后，你最想继续复利哪一件事？"
  }
];

const LESSONS_WARMUP = [
  {
    day: 1,
    title: "Catching Up",
    theme: "见面寒暄",
    idea: "把日常打招呼听成整块，不要听单词。",
    passage:
      "Hey, long time no see! How have you been? It's been a while. I keep meaning to drop by, but things have been crazy on my end. Let's grab coffee sometime and catch up properly. What have you been up to lately? Oh, just the usual, hanging in there. Honestly, it's so good to see you again. Let me know when you're free this week and we'll figure out a time.",
    zh:
      "今天先把见面打招呼最高频的几块整体吞下：long time no see / catch up / hang in there / drop by / what have you been up to。",
    phrases: [
      ["long time no see", "好久不见", "long time | no see——一长串日子+没碰面，中式直译反成惯用"],
      ["how have you been", "你近来怎么样", "have been = 一直处在——问的是这阵子的状态，不是此刻"],
      ["it's been a while", "有阵子没见了", "已经过了 a while——a while 是一段不长不短的时间"],
      ["drop by", "顺路过来一下", "drop = 放下，by = 路过——经过时把自己短暂\"丢\"在你这"],
      ["catch up", "叙叙旧 / 聊聊近况", "catch = 追上，up = 到位——把没聊到的近况一路追平"],
      ["what have you been up to", "你最近在忙什么", "up to = 投入到——你这阵子一直\"扑\"在什么上"],
      ["hanging in there", "还撑着 / 还行吧", "整个人挂在那儿——没掉下来也没爬上去"],
      ["good to see you", "见到你真好", "能看见你这件事本身就好"],
      ["the usual", "老样子", "那一份\"常点的\"——不用说菜名，对方知道是哪个"],
      ["figure out a time", "约一个时间", "figure = 把形状画出来——把\"什么时候\"在脑里描清"],
      ["grab coffee", "去喝杯咖啡", "grab = 一把抓——顺手拎一杯，不是慢慢喝"],
      ["on my end", "我这边", "end = 一端——像电话线两头，我这一头"],
      ["meaning to", "一直想要", "心里一直 mean 着要——意图持续但还没做"],
      ["let me know", "告诉我一声", "let me know——让我处于\"已知道\"的状态"],
      ["sometime this week", "这周找个时间", "some + time——这周的某一刻，不指定"]
    ],
    sentences: [
      ["Long time no see! How have you been?", "好久不见，你最近怎么样？", "long time no see 是一个固定整块，连读不要拆。"],
      ["I keep meaning to drop by, but things have been crazy on my end.", "我一直想顺路来看你，但我这边一直很忙。", "keep meaning to 是反复想做却没做；on my end 是\"我这边\"。"],
      ["Let me know when you're free and we'll figure out a time.", "你有空告诉我，我们再定时间。", "let me know 弱读，figure out a time 整块出来。"]
    ],
    reflection: "今天有哪一个语块，你过去会听成单词、现在能听成整块？"
  },
  {
    day: 2,
    title: "At Work",
    theme: "工作沟通",
    idea: "邮件/会议里最常被听漏的过渡语块，整组吞下。",
    passage:
      "Hey, just wanted to give you a heads up — I'll loop you in on the email. Can we circle back on this tomorrow? I want to make sure we're on the same page before we move forward. Also, please cc me when you send it out. If you need anything from my side, just shoot me a message. Let's touch base end of the day and wrap this up.",
    zh:
      "今天 heads up / loop in / circle back / on the same page / touch base 这一组是邮件和会议里被听漏最多的过渡语块。",
    phrases: [
      ["give you a heads up", "提前给你打个招呼", "heads up = 把头抬起来——前面有情况，先看一眼"],
      ["loop you in", "把你拉进这件事", "loop = 圈，in = 进来——把你套进这个圈里"],
      ["circle back", "回头再谈", "绕一个圆回到这一点——现在跳过，待会儿绕回"],
      ["on the same page", "想法一致", "翻开的是同一页书——你看到的字和我一模一样"],
      ["move forward", "继续推进", "往前挪一步——不是飞速，是迈一步"],
      ["cc me", "邮件抄送我", "carbon copy 的字母音——抄送名单加上我"],
      ["from my side", "从我这边", "side = 边——一桌人不同位置，我这一边"],
      ["shoot me a message", "发我一条消息", "shoot = 射——朝我射一条消息过来"],
      ["touch base", "碰个头", "棒球摸一下垒——只是\"我到了\"的确认，不停留"],
      ["wrap this up", "把这件事收尾", "像包礼物一样把它包起来——包好就能收走"],
      ["end of the day", "今天结束前", "end = 一天的尽头——收尾的时间点"],
      ["follow up on", "跟进", "跟着这件事的尾巴——up 走到底，on 贴着"],
      ["take the lead", "牵头负责", "lead = 牵绳——谁拿绳谁在前"],
      ["push back on", "反对 / 推回去", "把它往回推一下——不是硬反对，是推回去"],
      ["drop the ball", "掉链子 / 没办好", "球掉了——接力赛接错球，谁掉的谁负责"]
    ],
    sentences: [
      ["Just wanted to give you a heads up — I'll loop you in on the email.", "只是想提前知会你一声，邮件里我会把你加进来。", "give you a heads up 整块；loop you in 中 you 弱读。"],
      ["I want to make sure we're on the same page before we move forward.", "推进之前我想确认我们想法一致。", "make sure we're 连读；on the same page 是核心整块。"],
      ["Let's touch base end of the day and wrap this up.", "今天结束前我们碰一下，把这件事收尾。", "touch base 不能听成 touch base ball；wrap this up 末尾上挑。"]
    ],
    reflection: "circle back 和 touch base 在你的工作里对应哪种场景？"
  },
  {
    day: 3,
    title: "Food and Coffee",
    theme: "餐饮购物",
    idea: "餐厅、咖啡店、便利店里高频被吞掉的口语整块。",
    passage:
      "Want to grab a bite? I'm starving. There's this new spot down the street — let's give it a shot. Is it for here or to go? Let's dine in, I'm not in a rush. The drinks are on the house tonight, by the way. We can split the bill or I'll just pick up the tab — your call. Honestly, anything sounds good to me, I'm easy.",
    zh:
      "餐厅里 for here or to go / on the house / split the bill / pick up the tab 这几块出现频率极高，但教科书几乎不教。",
    phrases: [
      ["grab a bite", "随便吃点", "grab = 抓，bite = 一口——抓一口吃的"],
      ["I'm starving", "我饿死了", "正在饿死——-ing 表持续，比 hungry 狠得多"],
      ["down the street", "这条街往前一点", "down = 顺向——沿着街继续往前，不一定下坡"],
      ["give it a shot", "试一下", "shot = 一发子弹——给这事一发，试一枪"],
      ["for here or to go", "在这吃还是带走", "for here = 留在这；to go = 往外走"],
      ["dine in", "店内用餐", "in = 在里面——在店里吃"],
      ["I'm not in a rush", "我不赶时间", "rush = 冲——我不在\"急冲\"这状态里"],
      ["on the house", "店家请客", "这一份算房子的——house = 店家"],
      ["split the bill", "AA 平分账单", "split = 劈开——把账单劈两半"],
      ["pick up the tab", "我来买单", "tab = 小账单——我把它捡起来，我付"],
      ["your call", "你决定", "call = 召唤/决定——这通电话你打"],
      ["sounds good", "听起来不错", "sound = 听感——这件事听着就 OK"],
      ["I'm easy", "我都行", "easy 形容自己——我没要求，怎么都行"],
      ["I'll pass", "我就不要了", "pass = 传/过——这一棒让它过去"],
      ["not really my thing", "不太是我喜欢的", "my thing = 我那一件——不在我的偏好里"]
    ],
    sentences: [
      ["Want to grab a bite? I'm starving.", "想去随便吃点吗？我饿死了。", "grab a bite 是固定整块，不要听成 grab a bait。"],
      ["Is it for here or to go?", "在这吃还是带走？", "for here or to go 是收银员标准提问，整块识别。"],
      ["We can split the bill or I'll just pick up the tab — your call.", "我们可以 AA，或者我来买单——你定。", "split the bill / pick up the tab 都是固定块。"]
    ],
    reflection: "下次去咖啡店，你能不能不思考就听出 for here or to go？"
  },
  {
    day: 4,
    title: "Schedules and Plans",
    theme: "时间约定",
    idea: "约时间、改时间、迟到、临时变动——高频但语速快的整块。",
    passage:
      "Are you free Thursday? Let me pencil you in. Actually, can we push it back to next week? Something came up. I'm running a bit late — start without me, I'll be there in ten. Sorry for the short notice, but I have to reschedule. Can we move it up to two? Take your time, I'm not going anywhere. Let me get back to you on that.",
    zh:
      "pencil in / push back / move up / on short notice / let me get back to you 这一组在工作和生活里都出现，听不懂时间安排就乱套。",
    phrases: [
      ["pencil you in", "暂时把你排进日程", "铅笔 = 可擦——先写进日程，可能要改"],
      ["push it back", "时间往后推", "把它往后推——日历上往后挪"],
      ["move it up", "时间往前提", "up 这里 = 日历更早的位置——往前提"],
      ["something came up", "临时有事", "有件事冒上来——up = 冒头出现"],
      ["running a bit late", "我有点迟到", "正在\"跑着晚一点\"——running 持续中"],
      ["I'll be there in ten", "我十分钟到", "in ten = 十分钟内——再过十分钟我就在那"],
      ["on short notice", "临时通知", "在\"短得很的通知\"里——notice = 提前知会"],
      ["reschedule", "改约时间", "re- = 再，schedule = 时间表——重新排"],
      ["take your time", "你慢慢来", "把时间拿走——你想用多少就用"],
      ["I'm not going anywhere", "我不急", "我哪也不去——意思是慢慢来"],
      ["let me get back to you", "我回头答复你", "back to = 回到——让我回头再走回来"],
      ["I'm tied up", "我抽不开身", "tied up = 被绑住——动弹不得"],
      ["right on time", "刚好准时", "right = 正/恰好——正好在时间点上"],
      ["about time", "终于", "差不多到这个时间——也用作\"早该如此\"的感叹"],
      ["any time works", "什么时候都行", "works = 跑得通——任何时间都能行"]
    ],
    sentences: [
      ["Can we push it back to next week? Something came up.", "可以推到下周吗？临时有点事。", "push it back 中 it 弱读到几乎听不见。"],
      ["I'm running a bit late — start without me.", "我有点迟到，你们先开始。", "running a bit late 整块，bit late 连读。"],
      ["Sorry for the short notice, but I have to reschedule.", "抱歉这么临时通知，但我要改时间。", "on short notice 等结构要听成整块。"]
    ],
    reflection: "push back 和 move up 你能立刻反应过来是往后还是往前吗？"
  },
  {
    day: 5,
    title: "Opinions and Maybes",
    theme: "表达观点",
    idea: "英语里表达\"也许、大概、说不准\"的整块远比 maybe 多。",
    passage:
      "I'd say it's worth a try, but honestly, it depends. From my perspective, it kind of makes sense, but I'm on the fence. Off the top of my head, I can think of two reasons. Take it with a grain of salt — I'm just guessing here. To be fair, both options have upsides. Honestly speaking, I'm leaning toward the second one, but it's your call.",
    zh:
      "I'd say / it depends / on the fence / off the top of my head / take it with a grain of salt 是英语里给意见时的核心整块，比 maybe / I think 更高频。",
    phrases: [
      ["I'd say", "我会说 / 我觉得", "would 软化语气——比 I say 客气一档"],
      ["it depends", "看情况", "depend = 吊在某件事上——它要看那件事怎样"],
      ["from my perspective", "在我看来", "perspective = 透视点——我站这个视角"],
      ["kind of makes sense", "有点道理", "sense = 理出头绪——有那么一点想得通"],
      ["on the fence", "拿不定主意", "站在篱笆上——不左不右，两边没下"],
      ["off the top of my head", "我随口一想", "从头顶随手摘下来——没准，随口说"],
      ["take it with a grain of salt", "别太当真", "配一小撮盐吃——加点怀疑，别全信"],
      ["to be fair", "公平地说", "fair = 公平——公平点讲"],
      ["honestly speaking", "说实话", "speaking 正在进行——老实讲话"],
      ["leaning toward", "倾向于", "lean = 倾斜——身子往这边歪过去"],
      ["it's your call", "你决定吧", "call = 决定——这通电话你来打"],
      ["worth a try", "值得一试", "worth = 配得上——配得上试一发"],
      ["for sure", "肯定的", "为了\"确定\"这件事——100%"],
      ["no doubt", "毫无疑问", "doubt = 怀疑——没有怀疑"],
      ["I'm just guessing", "我只是猜的", "正在猜——-ing 表当下"]
    ],
    sentences: [
      ["From my perspective, it kind of makes sense, but I'm on the fence.", "在我看来有点道理，但我还在犹豫。", "kind of 弱读成 kinda；on the fence 整块。"],
      ["Off the top of my head, I can think of two reasons.", "我随口想，能想到两个理由。", "off the top of my head 是固定块，整体识别。"],
      ["Take it with a grain of salt — I'm just guessing here.", "别太当真——我只是随口说说。", "take it with a grain of salt 整块，不要拆 grain 和 salt。"]
    ],
    reflection: "你今天在中文里说\"看情况\"的时候，能脱口而出 it depends 吗？"
  },
  {
    day: 6,
    title: "Asking and Clarifying",
    theme: "求助澄清",
    idea: "听不懂、需要重复、需要解释——这一组是听力卡壳时最有用的整块。",
    passage:
      "Sorry, could you walk me through that one more time? I'm not sure I follow. Could you give me a hand with this? I just can't wrap my head around it. What do you mean by \"throughput\"? Could you give me an example? Let me see if I got this right. Fill me in when you have a minute. I'll give it a shot, but no promises.",
    zh:
      "walk me through / not sure I follow / wrap my head around / what do you mean by / fill me in 是英语对话里求澄清的标准整块。",
    phrases: [
      ["walk me through", "一步一步讲一遍", "陪我一步步走一遍——像导游带路"],
      ["one more time", "再一次", "再一遍——不是\"再几次\""],
      ["I'm not sure I follow", "我没太跟上", "follow = 跟在后面——我不确定跟上了"],
      ["give me a hand", "帮我个忙", "借我一只手——帮忙的具象说法"],
      ["wrap my head around", "想明白", "用头把它包起来——想通了才包得住"],
      ["what do you mean by", "你说的什么意思", "mean = 指向——你说这个指什么"],
      ["give me an example", "举个例子", "抽象→具象的桥——给一个例子"],
      ["if I got this right", "我理解对不对", "got = 抓到——我抓对了吗"],
      ["fill me in", "把情况告诉我", "把我灌满——把信息填进来"],
      ["when you have a minute", "你有空的时候", "你有那一分钟的时候——不急"],
      ["give it a shot", "试一下", "shot = 一发——给这事一枪试试"],
      ["no promises", "不能保证", "不打包票——不承诺成"],
      ["lend a hand", "搭把手", "把一只手借给——比 give 更轻，借完还"],
      ["no clue", "完全没头绪", "clue = 线索——一条都没"],
      ["get the hang of", "找到窍门", "hang = 挂着的把手——抓住它的窍门"]
    ],
    sentences: [
      ["Sorry, could you walk me through that one more time?", "不好意思，能再带我过一遍吗？", "walk me through 整块，me through 连读。"],
      ["I just can't wrap my head around it.", "我就是想不明白这件事。", "wrap my head around 是固定块。"],
      ["Fill me in when you have a minute.", "你有空把情况告诉我。", "fill me in 整块，me in 连读到几乎一个音。"]
    ],
    reflection: "下次听不懂时，你会用 \"I'm not sure I follow\" 还是 \"What?\""
  },
  {
    day: 7,
    title: "Pivots and Trade-offs",
    theme: "转折让步",
    idea: "口语里的转折远比 but 多——这组整块是听懂逻辑变化的关键。",
    passage:
      "I get your point. That being said, there's another side to consider. On the other hand, the cost is real. Fair enough — point taken. Come to think of it, we tried this last year and it didn't quite work. Then again, things have changed. It's not that simple; there's more to it. I see where you're coming from, but let's agree to disagree on this one.",
    zh:
      "that being said / on the other hand / fair enough / point taken / come to think of it 是英语里转折让步的高频整块，比 but 自然得多。",
    phrases: [
      ["I get your point", "我懂你的意思", "point = 尖头/要点——我抓到你的点了"],
      ["that being said", "话虽如此", "那句话已经说出来了——接转折"],
      ["another side to consider", "还有另一面要考虑", "side = 面——多面体里还有一面"],
      ["on the other hand", "另一方面", "在另一只手上——一手一边比较"],
      ["fair enough", "有道理 / 也行", "够公平了——没意见，接受"],
      ["point taken", "你说的有道理", "take = 接住——你的点我接住了"],
      ["come to think of it", "现在想想", "字面是\"来到想起这事\"——开始想起来"],
      ["didn't quite work", "效果不太好", "quite = 完全，work = 跑得通——没完全走通"],
      ["then again", "话又说回来", "再说一遍——表示转回头说"],
      ["it's not that simple", "没那么简单", "that = 那种程度——没有\"那么\"简单"],
      ["there's more to it", "事情没这么简单", "它后面还有更多——表面之外有内容"],
      ["I see where you're coming from", "我理解你的角度", "看见你从哪走来——你的来路即你的角度"],
      ["agree to disagree", "各保留看法", "同意彼此不同意——双方都不动"],
      ["on second thought", "再想想", "在第二次想的时候——重新想想后"],
      ["having said that", "话虽如此", "说完那句话之后——接让步转折"]
    ],
    sentences: [
      ["That being said, there's another side to consider.", "话虽如此，还有另一面要考虑。", "that being said 整块开头，常用于让步。"],
      ["Come to think of it, we tried this last year.", "现在想想，我们去年试过这个。", "come to think of it 整块作为思维转折标志。"],
      ["I see where you're coming from, but let's agree to disagree.", "我理解你的角度，但我们各自保留看法吧。", "where you're coming from 整块，是\"你的角度\"。"]
    ],
    reflection: "七天热身之后，下一步进入 30 天 Naval。你的耳朵准备好了吗？"
  }
];

const STORAGE_KEY = "naval-listening-state-v2";
const doubaoBasePrompt = `请扮演“硅谷思想视频英语听力教练”，学习者是中国大学英语四级水平，主要目标是听懂 Naval Ravikant、AI、风险投资、硅谷创业和国际科技视频。

请把我给你的英文材料改造成 10-15 分钟听力训练课，难度接近真实 CNBC / YC / TechCrunch / podcast 原文，不要改得太简单。训练流程必须贯彻四步：盲听优先、精听对照、同步理解、口语复刻。

请严格输出：
1. 盲听材料：一段 80-120 词英文，先不要给中文
2. 精听对照：同一段英文字幕
3. 同步理解：5 个值得反复听的英文原句 + 听到后应立即出现的英文意思/画面提示
4. 口语复刻：3 句最适合跟读模仿的原句 + 重音/连读/弱读提示
5. 8 个高价值英文语块：英文 + 中文 + 使用场景
6. 最后才给一句中文主旨和 3 个中文复述问题
7. 1 个今天最值得记住的 Naval 式思想

英文材料如下：`;

const videoPromptTemplate = (videoUrl, extraText) => `请扮演“硅谷思想视频英语听力教练”。学习者是中国大学英语四级水平，主要目标是听懂 Naval Ravikant、AI、风险投资、硅谷创业和国际科技视频。请优先保持真实英文视频的难度，不要改成过度简单的英语。

请根据下面的视频链接，帮我做成 10-15 分钟英语听力学习资料。训练流程必须贯彻四步：盲听优先、精听对照、同步理解、口语复刻。

视频链接：
${videoUrl}

${extraText ? `我补充的字幕或背景如下：\n${extraText}\n` : "如果你无法直接读取视频字幕，请告诉我如何复制字幕；如果能读取，请直接处理。\n"}
请严格输出：
1. 盲听材料：一段 80-120 词英文，先不要给中文
2. 精听对照：同一段英文字幕
3. 同步理解：5 个值得反复听的英文原句 + 听到后应立即出现的英文意思/画面提示
4. 口语复刻：3 句最适合跟读模仿的原句 + 重音/连读/弱读提示
5. 8 个高价值英文语块：英文 + 中文 + 使用场景
6. 最后才给一句中文主旨和 3 个中文复述问题
7. 1 个最值得记住的 Naval 式思想
8. 最后给我一个“今天只学 10 分钟”的学习顺序

请把输出写得适合复制回我的 Naval 英语听力 App 使用。`;

let state = loadState();
let selectedLessonDay = getActiveSelectedDay();
let toastTimer = null;
let activeShadowButton = null;
let mediaRecorder = null;
let recordedChunks = [];
let recordingStream = null;
let recordingForText = "";

const views = document.querySelectorAll(".view");
const navItems = document.querySelectorAll(".nav-item");
const lessonPanel = document.getElementById("lessonPanel");
const lessonList = document.getElementById("lessonList");
const phraseList = document.getElementById("phraseList");
const reviewPanel = document.getElementById("reviewPanel");
const toast = document.getElementById("toast");

function loadState() {
  const defaults = {
    schemaVersion: 4,
    completed: {},
    notes: {},
    blindNotes: {},
    lessonStages: {},
    phrases: [],
    selectedLessonDay: 1,
    sessionMinutes: 10,
    reviewLog: {},
    activeTrack: "warmup",
    warmupCompleted: {},
    warmupStages: {},
    warmupSelectedDay: 1,
    blindAccuracy: {},
    spokenChunks: [],
    streak: 0,
    longestStreak: 0,
    lastCompletedDate: null,
    bonusLinks: {},
    highlightSeenForKey: null,
    xp: 0,
    level: 1,
    achievements: [],
    trainingHistory: [],
    coachMode: true,
    lastXpEvents: []
  };

  let saved = {};
  try {
    const legacy = JSON.parse(localStorage.getItem("naval-listening-state-v1"));
    if (legacy && !localStorage.getItem(STORAGE_KEY)) saved = legacy;
  } catch {}
  try {
    const current = JSON.parse(localStorage.getItem(STORAGE_KEY));
    if (current) saved = current;
  } catch {}

  const merged = { ...defaults, ...saved };
  return migrateState(merged);
}

function migrateState(s) {
  if (!s.schemaVersion || s.schemaVersion < 2) {
    const oldLog = s.reviewLog || {};
    const newLog = {};
    Object.entries(oldLog).forEach(([en, value]) => {
      if (value && typeof value === "object" && "dueDate" in value) {
        newLog[en] = value;
        return;
      }
      const lastDate = typeof value === "string" ? value : new Date().toDateString();
      newLog[en] = {
        ef: 2.5,
        interval: 1,
        repetitions: 1,
        dueDate: toIsoDay(new Date(lastDate)),
        lastReviewed: toIsoDay(new Date(lastDate)),
        mastery: "听过"
      };
    });
    s.reviewLog = newLog;
    if (s.activeTrack == null) s.activeTrack = "warmup";
    if (!s.warmupCompleted) s.warmupCompleted = {};
    if (!s.warmupStages) s.warmupStages = {};
    if (!s.warmupSelectedDay) s.warmupSelectedDay = 1;
    if (!s.blindAccuracy) s.blindAccuracy = {};
    if (!Array.isArray(s.spokenChunks)) s.spokenChunks = [];
    s.schemaVersion = 2;
  }
  if (s.schemaVersion < 3) {
    if (typeof s.streak !== "number") s.streak = 0;
    if (typeof s.longestStreak !== "number") s.longestStreak = 0;
    if (typeof s.lastCompletedDate === "undefined") s.lastCompletedDate = null;
    if (!s.bonusLinks) s.bonusLinks = {};
    if (typeof s.highlightSeenForKey === "undefined") s.highlightSeenForKey = null;
    s.schemaVersion = 3;
  }
  if (s.schemaVersion < 4) {
    if (typeof s.xp !== "number") s.xp = 0;
    if (typeof s.level !== "number") s.level = 1;
    if (!Array.isArray(s.achievements)) s.achievements = [];
    if (!Array.isArray(s.trainingHistory)) s.trainingHistory = [];
    if (typeof s.coachMode === "undefined") s.coachMode = true;
    if (!Array.isArray(s.lastXpEvents)) s.lastXpEvents = [];
    s.schemaVersion = 4;
  }
  return s;
}

// === 私人教练：等级 / XP / 成就 ===

const LEVEL_LADDER = [
  { level: 1, xpToNext: 100, name: "日常对话起步", desc: "见面寒暄、餐厅约时、工作流转" },
  { level: 2, xpToNext: 250, name: "生活英语稳了", desc: "听得懂 Friends 慢速、能完整应对常见场景" },
  { level: 3, xpToNext: 500, name: "思想访谈级", desc: "Naval 类播客踮脚能跟、抽象词汇连读破壳" },
  { level: 4, xpToNext: 900, name: "时政新闻级", desc: "CNN/BBC 新闻速度 + 行业词汇能听懂主线" },
  { level: 5, xpToNext: 1500, name: "脱口秀文化级", desc: "Last Week Tonight / 博客对话 / 文化双关秒懂" },
  { level: 6, xpToNext: 0, name: "听感自由", desc: "外语在你脑中和母语反应一样快" }
];

const ACHIEVEMENTS = [
  { id: "first_phrase", title: "第一个语块入库", hint: "保存第 1 个语块到语块本", icon: "🌱" },
  { id: "phrase_25", title: "语块小宇宙", hint: "累计存了 25 个语块", icon: "📚" },
  { id: "phrase_100", title: "语块图书馆", hint: "累计存了 100 个语块", icon: "🏛" },
  { id: "first_recording_ok", title: "第一次开口", hint: "录音对照后点了「这条满意」", icon: "🎙" },
  { id: "spoken_10", title: "开口 10 块", hint: "「能用出来」语块达到 10 个", icon: "🗣" },
  { id: "spoken_50", title: "开口 50 块", hint: "「能用出来」语块达到 50 个", icon: "🔥" },
  { id: "streak_3", title: "连续 3 天", hint: "连续打卡 3 天，节奏起来了", icon: "⚡" },
  { id: "streak_7", title: "连续 7 天", hint: "连续打卡 7 天，80% 的人停在这里之前", icon: "💪" },
  { id: "streak_21", title: "连续 21 天", hint: "连续 21 天，习惯长出来了", icon: "🌳" },
  { id: "level_2", title: "升到 L2", hint: "听感升到「生活英语稳了」", icon: "🎯" },
  { id: "level_3", title: "升到 L3", hint: "听感升到「思想访谈级」", icon: "🚀" },
  { id: "level_4", title: "升到 L4", hint: "听感升到「时政新闻级」", icon: "🛰" },
  { id: "first_blind_full", title: "首次「全懂」", hint: "首遍盲听就听懂了一整段", icon: "✨" },
  { id: "session_30", title: "30 次训练", hint: "累计完成 30 次碎片训练", icon: "⛰" }
];

function xpToNextLevel(level) {
  const tier = LEVEL_LADDER.find((t) => t.level === level);
  return tier ? tier.xpToNext : 0;
}

function getLevelInfo(level) {
  return LEVEL_LADDER.find((t) => t.level === level) || LEVEL_LADDER[LEVEL_LADDER.length - 1];
}

function awardXp(amount, reason) {
  if (!amount || amount <= 0) return [];
  state.xp = (state.xp || 0) + amount;
  state.lastXpEvents = state.lastXpEvents || [];
  state.lastXpEvents.unshift({ amount, reason, at: Date.now() });
  state.lastXpEvents = state.lastXpEvents.slice(0, 20);
  const unlocked = [];
  while (true) {
    const need = xpToNextLevel(state.level);
    if (!need || state.xp < need) break;
    state.xp -= need;
    state.level += 1;
    const lvlAch = ACHIEVEMENTS.find((a) => a.id === `level_${state.level}`);
    if (lvlAch) unlocked.push(lvlAch);
    unlocked.push({ id: `__levelup_${state.level}`, title: `升到 L${state.level}`, hint: getLevelInfo(state.level).name, icon: "🆙" });
  }
  saveState();
  return unlocked;
}

function checkAchievements() {
  const have = new Set(state.achievements || []);
  const newly = [];
  const conditions = [
    ["first_phrase", () => (state.phrases || []).length >= 1],
    ["phrase_25", () => (state.phrases || []).length >= 25],
    ["phrase_100", () => (state.phrases || []).length >= 100],
    ["first_recording_ok", () => (state.spokenChunks || []).length >= 1],
    ["spoken_10", () => (state.spokenChunks || []).length >= 10],
    ["spoken_50", () => (state.spokenChunks || []).length >= 50],
    ["streak_3", () => (state.streak || 0) >= 3],
    ["streak_7", () => (state.streak || 0) >= 7],
    ["streak_21", () => (state.streak || 0) >= 21],
    ["level_2", () => state.level >= 2],
    ["level_3", () => state.level >= 3],
    ["level_4", () => state.level >= 4],
    ["first_blind_full", () => Object.values(state.blindAccuracy || {}).some((a) => a.first === "全懂")],
    ["session_30", () => (state.trainingHistory || []).length >= 30]
  ];
  conditions.forEach(([id, ok]) => {
    if (!have.has(id) && ok()) {
      have.add(id);
      const def = ACHIEVEMENTS.find((a) => a.id === id);
      if (def) newly.push(def);
    }
  });
  state.achievements = [...have];
  return newly;
}

function flashRewards(xpAmount, xpReason, unlockedAchievements) {
  const toaster = document.getElementById("rewardToaster");
  if (!toaster) return;
  const ts = Date.now();
  const items = [];
  if (xpAmount > 0) items.push(`<div class="reward-chip xp"><span class="reward-amount">+${xpAmount} XP</span><span class="reward-reason">${escapeHtml(xpReason || "")}</span></div>`);
  unlockedAchievements.forEach((a) => {
    items.push(`<div class="reward-chip achv"><span class="reward-icon">${a.icon || "🏅"}</span><div><span class="reward-title">${escapeHtml(a.title)}</span><span class="reward-reason">${escapeHtml(a.hint || "")}</span></div></div>`);
  });
  if (!items.length) return;
  const id = "rwd_" + ts;
  const wrap = document.createElement("div");
  wrap.className = "reward-burst";
  wrap.id = id;
  wrap.innerHTML = items.join("");
  toaster.appendChild(wrap);
  setTimeout(() => {
    wrap.classList.add("fade");
    setTimeout(() => wrap.remove(), 600);
  }, 3200);
}

function rewardAction(xpAmount, xpReason) {
  const lvlUnlocks = awardXp(xpAmount, xpReason);
  const newAchievements = checkAchievements();
  saveState();
  flashRewards(xpAmount, xpReason, [...newAchievements, ...lvlUnlocks]);
  updateCoachPanel();
  updateStreakBadge();
  if (newAchievements.length > 0) renderAchievementWall();
}

function startQuickSession(minutes) {
  state.trainingHistory = state.trainingHistory || [];
  state.trainingHistory.unshift({ kind: "session_start", minutes, at: Date.now() });
  state.trainingHistory = state.trainingHistory.slice(0, 200);
  saveState();
  const dueCount = getDueReviews().length;
  if (minutes === 5) {
    if (dueCount > 0) {
      openView("reviewView");
      showToast(`5 分钟训练：先复习 ${Math.min(dueCount, 3)} 张到期卡。`);
    } else {
      state.sessionMinutes = 5;
      openView("todayView");
      saveState();
      renderAll();
      showToast("5 分钟训练：跑一段盲听 → 终听验证。");
    }
    rewardAction(5, "5 分钟训练开局");
  } else if (minutes === 10) {
    state.sessionMinutes = 10;
    openView("todayView");
    saveState();
    renderAll();
    showToast("10 分钟训练：走完盲听→精听→同步→录音→终听。");
    rewardAction(8, "10 分钟训练开局");
  } else if (minutes === 15) {
    state.sessionMinutes = 15;
    openView("todayView");
    saveState();
    renderAll();
    showToast(`15 分钟深度训练：${dueCount > 0 ? `先去复习 ${Math.min(dueCount, 5)} 张，` : ""}然后跑完整段。`);
    rewardAction(12, "15 分钟训练开局");
  }
}

function toIsoDay(date) {
  const d = date instanceof Date && !isNaN(date) ? date : new Date();
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, "0");
  const day = String(d.getDate()).padStart(2, "0");
  return `${y}-${m}-${day}`;
}

function todayIso() {
  return toIsoDay(new Date());
}

function addDays(isoDay, n) {
  const d = new Date(isoDay + "T00:00:00");
  d.setDate(d.getDate() + Math.round(n));
  return toIsoDay(d);
}

function saveState() {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
}

function getActiveLessons() {
  return state.activeTrack === "naval" ? LESSONS : LESSONS_WARMUP;
}

function getCompletedMap() {
  return state.activeTrack === "naval" ? state.completed : state.warmupCompleted;
}

function getStagesMap() {
  return state.activeTrack === "naval" ? state.lessonStages : state.warmupStages;
}

function getActiveSelectedDay() {
  const stored = state.activeTrack === "naval" ? state.selectedLessonDay : state.warmupSelectedDay;
  if (stored) return Number(stored);
  return getFirstOpenDay();
}

function setActiveSelectedDay(day) {
  if (state.activeTrack === "naval") state.selectedLessonDay = day;
  else state.warmupSelectedDay = day;
}

function getFirstOpenDay() {
  const lessons = getActiveLessons();
  const completedMap = getCompletedMap();
  const first = lessons.find((lesson) => !completedMap[String(lesson.day)]);
  return first ? first.day : lessons[lessons.length - 1].day;
}

function getLesson(day) {
  const lessons = getActiveLessons();
  return lessons.find((lesson) => lesson.day === Number(day)) || lessons[0];
}

function getLessonStage(day) {
  const stages = getStagesMap();
  return Math.min(Math.max(Number(stages[String(day)] || 1), 1), 5);
}

function setLessonStage(day, stage) {
  const requested = Math.min(Math.max(Number(stage), 1), 5);
  const current = getLessonStage(day);
  if (requested > current + 1) {
    showToast("请按顺序进入下一阶段，不要跳过盲听。");
    return;
  }
  if (requested === 2 && !(state.blindAccuracy[stageKey(day)] || {}).first) {
    showToast("先选一下你刚才盲听到什么程度。");
    return;
  }
  saveLessonDrafts();
  const stages = getStagesMap();
  stages[String(day)] = requested;
  saveState();
  renderToday();
}

function stageKey(day) {
  return `${state.activeTrack}-${day}`;
}

function isWarmupComplete() {
  return LESSONS_WARMUP.every((lesson) => state.warmupCompleted[String(lesson.day)]);
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function showToast(message) {
  window.clearTimeout(toastTimer);
  toast.textContent = message;
  toast.classList.add("show");
  toastTimer = window.setTimeout(() => toast.classList.remove("show"), 2200);
}

function speak(text, rate = 0.86) {
  if (!("speechSynthesis" in window)) {
    showToast("这个浏览器暂时不能朗读。");
    return;
  }

  window.speechSynthesis.cancel();
  const utterance = new SpeechSynthesisUtterance(text);
  utterance.lang = "en-US";
  utterance.rate = rate;
  utterance.pitch = 1;
  const voices = window.speechSynthesis.getVoices();
  // 优先级：(Enhanced/Premium) > Siri/Neural > 已知好声音 > 任何 en-US
  const tiers = [
    /(Premium|Enhanced).*en[-_]US/i,
    /en[-_]US.*(Premium|Enhanced)/i,
    /(Siri|Neural|Natural)/i,
    /(Ava|Allison|Samantha|Joelle|Nicky|Karen)/i,
    /Google US English/i,
    /en[-_]US/i
  ];
  let preferred = null;
  for (const re of tiers) {
    preferred = voices.find((v) => re.test(v.name) || (v.lang && re.test(v.lang)));
    if (preferred) break;
  }
  if (preferred) utterance.voice = preferred;
  window.speechSynthesis.speak(utterance);
}

function renderAll() {
  renderToday();
  renderCourseList();
  renderPhrases();
  renderReview();
  updateSessionChips();
  updateProgress();
  updateStreakBadge();
  refreshBackupStatusLine();
  renderAchievementWall();
}

function renderAchievementWall() {
  const wall = document.getElementById("achievementWall");
  if (!wall) return;
  const have = new Set(state.achievements || []);
  const items = ACHIEVEMENTS.map((a) => {
    const got = have.has(a.id);
    return `
      <div class="ach-card ${got ? "ach-got" : "ach-locked"}" title="${escapeHtml(a.hint || "")}">
        <span class="ach-icon">${got ? a.icon : "🔒"}</span>
        <div>
          <span class="ach-title">${escapeHtml(a.title)}</span>
          <span class="ach-hint">${escapeHtml(a.hint || "")}</span>
        </div>
      </div>
    `;
  }).join("");
  const gotCount = have.size;
  wall.innerHTML = `
    <div class="ach-head">
      <p class="eyebrow">成就墙</p>
      <span class="ach-count">${gotCount}/${ACHIEVEMENTS.length}</span>
    </div>
    <div class="ach-grid">${items}</div>
  `;
}

function updateStreakBadge() {
  const el = document.getElementById("streakCount");
  const badge = document.getElementById("streakBadge");
  if (!el || !badge) return;
  const today = todayIso();
  const yesterday = addDays(today, -1);
  let live = state.streak || 0;
  if (state.lastCompletedDate && state.lastCompletedDate !== today && state.lastCompletedDate !== yesterday) live = 0;
  el.textContent = live;
  badge.classList.toggle("streak-cold", live === 0);
  badge.classList.toggle("streak-hot", live >= 3);
  badge.classList.toggle("streak-blaze", live >= 7);
  badge.setAttribute("title", live === 0
    ? "今天打卡就开始连续记录"
    : state.lastCompletedDate === today
    ? `今天已完成，已连续 ${live} 天`
    : `昨天最后一次打卡，连续 ${live} 天还能续上`);
}

function updateProgress() {
  renderDashboard();
  updateCoachPanel();
}

function updateCoachPanel() {
  const panel = document.getElementById("coachPanel");
  if (!panel) return;
  const info = getLevelInfo(state.level);
  const next = LEVEL_LADDER.find((t) => t.level === state.level + 1);
  const need = xpToNextLevel(state.level) || 0;
  const pct = need > 0 ? Math.min(100, Math.round((state.xp / need) * 100)) : 100;
  const spokenCount = (state.spokenChunks || []).length;
  const phraseCount = (state.phrases || []).length;
  const sessionCount = (state.trainingHistory || []).length;
  panel.innerHTML = `
    <div class="coach-head">
      <div>
        <p class="eyebrow">你的私人英语教练</p>
        <h2 class="coach-level">L${state.level} · ${escapeHtml(info.name)}</h2>
        <p class="muted coach-desc">${escapeHtml(info.desc)}</p>
      </div>
      <div class="coach-xp-circle" aria-label="经验值">
        <span class="coach-xp-num">${state.xp}</span>
        <span class="coach-xp-of">/${need || "MAX"}</span>
      </div>
    </div>
    <div class="coach-bar"><div class="coach-bar-fill" style="width:${pct}%;"></div></div>
    ${next ? `<p class="coach-next">还差 <strong>${Math.max(0, need - state.xp)}</strong> XP 升到 L${next.level} · ${escapeHtml(next.name)}</p>` : `<p class="coach-next">你已经在顶级，继续巩固。</p>`}
    <div class="quick-train">
      <p class="eyebrow">现在就练</p>
      <div class="quick-buttons">
        <button class="quick-btn" data-quick-train="5">
          <span class="qt-min">5 分</span>
          <span class="qt-detail">复习 1 + 新学 1 + 1 段盲听</span>
        </button>
        <button class="quick-btn primary-quick" data-quick-train="10">
          <span class="qt-min">10 分</span>
          <span class="qt-detail">复习 3 + 新学 2 + 1 段全流程</span>
        </button>
        <button class="quick-btn" data-quick-train="15">
          <span class="qt-min">15 分</span>
          <span class="qt-detail">复习 5 + 新学 3 + passage + 录音</span>
        </button>
      </div>
    </div>
    <div class="coach-stats">
      <div><strong>${spokenCount}</strong><span>能开口</span></div>
      <div><strong>${phraseCount}</strong><span>语块累计</span></div>
      <div><strong>${sessionCount}</strong><span>训练次数</span></div>
    </div>
    <button class="conversation-coach-cta" id="conversationCoachButton">
      <span class="ccc-icon">🎤</span>
      <span class="ccc-body">
        <span class="ccc-title">找豆包当你的口语对话教练</span>
        <span class="ccc-hint">复制为你定制的 prompt，去豆包用语音对话练真嘴皮 →</span>
      </span>
    </button>
  `;
}

function renderDashboard() {
  const container = document.getElementById("dashboardCards");
  if (!container) return;

  const blindData = getRecentBlindAccuracy(7);
  const masteryData = getMasteryDistribution();
  const spokenWeek = getSpokenChunksThisWeek();
  const totalChunks = state.spokenChunks.length;

  const accuracyColors = {
    "全懂": "var(--color-accent-strong, #1f9d55)",
    "懂大意": "var(--color-accent, #38c172)",
    "几个词": "var(--color-warn, #f5a623)",
    "没听懂": "var(--color-soft, #d4d8df)"
  };

  const blindBars = blindData
    .map(({ date, level }) => {
      const color = level ? accuracyColors[level] : "var(--color-soft, #e4e7eb)";
      const height = level === "全懂" ? 100 : level === "懂大意" ? 75 : level === "几个词" ? 45 : level === "没听懂" ? 20 : 6;
      const label = date.slice(5);
      return `<div class="bar-slot"><div class="bar" style="height:${height}%; background:${color};" title="${label} · ${level || "未练"}"></div><span>${label}</span></div>`;
    })
    .join("");

  const masteryTotal = Math.max(1, masteryData.陌生 + masteryData.听过 + masteryData.一听就懂 + masteryData.能用出来);
  const seg = (n, color) => `<span class="stack-seg" style="flex:${n}; background:${color};" title="${n}"></span>`;
  const masteryStack = `
    ${seg(masteryData.陌生, "var(--color-soft, #d4d8df)")}
    ${seg(masteryData.听过, "#f5a623")}
    ${seg(masteryData.一听就懂, "#38c172")}
    ${seg(masteryData.能用出来, "#3490dc")}
  `;

  container.innerHTML = `
    <div class="dashboard-card">
      <p class="eyebrow">本周首遍盲听</p>
      <div class="bar-chart">${blindBars || "<span class='muted'>开始第一课就有数据</span>"}</div>
      <p class="card-foot muted">${blindData.filter((d) => d.level).length} / 7 天有数据</p>
    </div>
    <div class="dashboard-card">
      <p class="eyebrow">语块熟练度</p>
      <div class="stacked-bar" aria-label="语块熟练度分布">${masteryStack}</div>
      <p class="card-foot muted">陌生 ${masteryData.陌生} · 听过 ${masteryData.听过} · 一听就懂 ${masteryData.一听就懂} · 能用 ${masteryData.能用出来}</p>
    </div>
    <div class="dashboard-card">
      <p class="eyebrow">能开口语块</p>
      <p class="big-number">${totalChunks}</p>
      <p class="card-foot muted">本周 +${spokenWeek}</p>
    </div>
  `;
}

function getRecentBlindAccuracy(days = 7) {
  const out = [];
  const today = new Date();
  for (let i = days - 1; i >= 0; i--) {
    const d = new Date(today);
    d.setDate(d.getDate() - i);
    const iso = toIsoDay(d);
    const records = Object.values(state.blindAccuracy).filter((r) => r && r.firstDate === iso);
    const level = records.length ? records[records.length - 1].first : null;
    out.push({ date: iso, level });
  }
  return out;
}

function getMasteryDistribution() {
  const dist = { 陌生: 0, 听过: 0, 一听就懂: 0, 能用出来: 0 };
  state.phrases.forEach((item) => {
    const record = state.reviewLog[item.en];
    let mastery = "陌生";
    if (record) {
      if (record.repetitions >= 3) mastery = "一听就懂";
      else if (record.repetitions >= 1) mastery = "听过";
    }
    if (state.spokenChunks.some((e) => (typeof e === "string" ? e : e.en) === item.en)) mastery = "能用出来";
    dist[mastery] = (dist[mastery] || 0) + 1;
  });
  return dist;
}

function getSpokenChunksThisWeek() {
  return state.spokenChunks.filter((entry) => {
    if (typeof entry === "string") return false;
    const at = entry && entry.at;
    if (!at) return false;
    const diff = (Date.now() - new Date(at).getTime()) / (1000 * 60 * 60 * 24);
    return diff <= 7;
  }).length;
}

function updateSessionChips() {
  document.querySelectorAll("[data-session]").forEach((button) => {
    button.classList.toggle("active", Number(button.dataset.session) === Number(state.sessionMinutes));
  });
}

function saveLessonDrafts() {
  const key = stageKey(selectedLessonDay);
  const blindBox = document.getElementById("blindNoteBox");
  if (blindBox) state.blindNotes[key] = blindBox.value.trim();

  const reflectionBox = document.getElementById("reflectionBox");
  if (reflectionBox) state.notes[key] = reflectionBox.value.trim();
}

function renderToday() {
  renderDashboard();
  const lesson = getLesson(selectedLessonDay);
  document.getElementById("currentDayNumber").textContent = lesson.day;
  const trackLabel = state.activeTrack === "warmup" ? "热身 · " : "Naval · ";
  document.getElementById("todaySubtitle").textContent = `${trackLabel}${lesson.title} · ${lesson.theme}`;
  const completedMap = getCompletedMap();
  const completed = Boolean(completedMap[String(lesson.day)]);
  const stage = getLessonStage(lesson.day);
  const key = stageKey(lesson.day);
  const savedNote = state.notes[key] || "";
  const savedBlindNote = state.blindNotes[key] || "";
  const lightMode = Number(state.sessionMinutes) === 5;
  const accuracy = state.blindAccuracy[key] || {};
  const stageNames = [
    ["盲听优先", "只听声音"],
    ["精听对照", "看英文字幕"],
    ["同步理解", "听到即懂"],
    ["口语复刻", "模仿输出"],
    ["终听验证", "再盲听一次"]
  ];
  const highlightLevel = accuracy.first || "";
  const showCn = stage >= 4;
  const phraseHtml = lesson.phrases
    .map(
      ([en, cn, intuition]) => {
        const highlight = stage === 2 && (highlightLevel === "懂大意" || highlightLevel === "几个词" || highlightLevel === "没听懂");
        const primary = intuition || cn;
        const secondary = intuition ? cn : "";
        return `
        <div class="phrase-card${highlight ? " phrase-highlight" : ""}">
          <div>
            <strong>${escapeHtml(en)}</strong>
            <span class="phrase-intuition">${showCn ? escapeHtml(primary) : "先用声音和英文场景理解，中文意思稍后再看。"}</span>
            ${showCn && secondary ? `<span class="phrase-cn-aside">${escapeHtml(secondary)}</span>` : ""}
          </div>
          <div class="phrase-actions">
            <button class="phrase-icon" data-deep-dive="${escapeHtml(en)}" data-meaning="${escapeHtml(cn)}" title="让豆包讲透这一个" aria-label="讲透 ${escapeHtml(en)}">
              <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9" /><path d="M12 8v5" /><circle cx="12" cy="16" r="0.6" fill="currentColor"/></svg>
            </button>
            <button class="phrase-icon" data-save-phrase="${escapeHtml(en)}" data-meaning="${escapeHtml(cn)}" data-intuition="${escapeHtml(intuition || "")}" title="存进语块本" aria-label="保存 ${escapeHtml(en)}">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 4h14v16l-7-3-7 3z" /></svg>
            </button>
          </div>
        </div>
      `;
      }
    )
    .join("");

  const sentenceItems = (lightMode ? lesson.sentences.slice(0, 2) : lesson.sentences)
    .map(([en, cn, tip], index) => {
      const sentenceNumber = index + 1;
      const isSpoken = state.spokenChunks.some((e) => (typeof e === "string" ? e : e.en) === en);
      return `
        <div class="sentence-card${isSpoken ? " sentence-spoken" : ""}">
          <p class="step-count">Sentence ${sentenceNumber}${isSpoken ? " · 已开口" : ""}</p>
          <p class="en">${escapeHtml(en)}</p>
          ${stage >= 4 ? `<p class="cn">${escapeHtml(cn)}</p>` : ""}
          <p class="tip">${stage >= 4 ? escapeHtml(tip) : "听完立刻在脑中出现意思，不要先翻译成中文。"}</p>
          <div class="sentence-actions">
            <button class="play-button" data-speak="${escapeHtml(en)}" data-rate="0.72">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
              慢速
            </button>
            <button class="play-button" data-speak="${escapeHtml(en)}" data-rate="0.95">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
              正常
            </button>
            ${
              stage >= 4
                ? `<button class="hold-button" data-record-text="${escapeHtml(en)}" data-record-index="${sentenceNumber}">
                    <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="6" /></svg>
                    按住录我念
                  </button>`
                : ""
            }
          </div>
        </div>
      `;
    })
    .join("");

  const stageStrip = stageNames
    .map(
      ([name, hint], index) => `
        <button class="method-step ${stage === index + 1 ? "active" : ""} ${stage > index + 1 ? "done" : ""}" data-set-stage="${index + 1}">
          <strong>${index + 1}. ${name}</strong>
          <span>${hint}</span>
        </button>
      `
    )
    .join("");

  const accuracyChips = ["全懂", "懂大意", "几个词", "没听懂"]
    .map(
      (level) => `
        <button class="accuracy-chip ${accuracy.first === level ? "active" : ""}" data-blind-accuracy="first" data-level="${level}">${level}</button>
      `
    )
    .join("");

  const blindBlock = `
    <section class="lesson-block active-training">
      <p class="eyebrow">Step 1</p>
      <h3>盲听优先</h3>
      <div class="blind-card">
        <p class="blind-title">先不要看英文，也不要急着翻译。只让耳朵接收声音。</p>
        <p class="hidden-text">字幕已隐藏</p>
      </div>
      <div class="action-row">
        <button class="primary" data-speak="${escapeHtml(lesson.passage)}" data-rate="0.95">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
          盲听正常速
        </button>
        <button class="secondary" data-speak="${escapeHtml(lesson.passage)}" data-rate="0.78">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
          再听慢速
        </button>
      </div>
      <textarea class="reflection-box" id="blindNoteBox" placeholder="可选：写下你听到的关键词或大意。不要查字幕。">${escapeHtml(savedBlindNote)}</textarea>
      <div class="self-assess">
        <p class="muted">刚才那一遍你听到什么程度？</p>
        <div class="chip-row">${accuracyChips}</div>
      </div>
      <button class="primary wide" data-set-stage="2" ${accuracy.first ? "" : "disabled"}>${accuracy.first ? "进入精听对照" : "选一档再继续"}</button>
    </section>
  `;

  const highlightedPassage = renderPassageWithHighlights(lesson, highlightLevel);
  const gapHint =
    highlightLevel === "全懂"
      ? "你说全懂——看英文核对一下，看看是不是真的连每个连读都听准了。"
      : highlightLevel === "懂大意"
      ? "你说懂大意——黄色的是核心语块，对一下刚才有没有听漏。"
      : highlightLevel === "几个词"
      ? "你说听到几个词——黄色的语块和橙色的句子，是你刚才大概率漏掉的。"
      : "你说没听懂——这是正常的，先把高亮的整块和原文匹配上，再回去盲听一次。";

  const subtitleBlock = `
    <section class="lesson-block active-training">
      <p class="eyebrow">Step 2</p>
      <h3>精听对照</h3>
      <p class="zh-note">${escapeHtml(gapHint)}</p>
      <p class="passage">${highlightedPassage}</p>
      <div class="action-row">
        <button class="primary" data-speak="${escapeHtml(lesson.passage)}" data-rate="0.86">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
          看英文再听
        </button>
        <button class="secondary" data-set-stage="1">回到盲听</button>
        <button class="secondary" data-set-stage="3">进入同步理解</button>
      </div>
    </section>

    <section class="lesson-block">
      <h3>先抓英文语块</h3>
      <div class="phrase-grid">${phraseHtml}</div>
    </section>
  `;

  const syncBlock = `
    <section class="lesson-block active-training">
      <p class="eyebrow">Step 3</p>
      <h3>同步理解</h3>
      <p class="zh-note">每句听完后，马上在脑中出现画面或意思。不要先译成中文；确认理解后再继续下一句。</p>
      <div class="sentence-list">${sentenceItems}</div>
      <div class="action-row">
        <button class="secondary" data-set-stage="2">回到精听</button>
        <button class="primary" data-set-stage="4">进入口语复刻</button>
      </div>
    </section>

    <section class="lesson-block">
      <h3>高价值语块</h3>
      <div class="phrase-grid">${phraseHtml}</div>
    </section>
  `;

  const shadowBlock = `
    <section class="lesson-block active-training">
      <p class="eyebrow">Step 4</p>
      <h3>口语复刻</h3>
      <p class="zh-note">先听一句，再按住"按住录我念"模仿说出来。会弹出对照框：把你的波形和原文音节节奏带叠在一起，自己耳朵当评分器。</p>
      <div class="sentence-list">${sentenceItems}</div>
      <div class="conversation-coach-inline">
        <p class="muted small">想要真人对答而不只是跟读？</p>
        <button class="secondary compact" id="shadowConversationCoachButton">
          <span style="font-size:18px;">🎤</span>
          找豆包用语音陪你对话练
        </button>
      </div>
      <div class="action-row">
        <button class="primary" data-set-stage="5">进入终听验证</button>
      </div>
    </section>

    <section class="lesson-block">
      <h3>语块收入语感库</h3>
      <div class="phrase-grid">${phraseHtml}</div>
    </section>
  `;

  const finalAccuracyChips = ["全懂", "懂大意", "几个词", "没听懂"]
    .map(
      (level) => `
        <button class="accuracy-chip ${accuracy.final === level ? "active" : ""}" data-blind-accuracy="final" data-level="${level}">${level}</button>
      `
    )
    .join("");

  const deltaHint =
    accuracy.first && accuracy.final
      ? accuracyDeltaSentence(accuracy.first, accuracy.final)
      : "再选一次你现在的听感，和最开始的盲听对照一下——这就是今天的正反馈。";

  const bonusKey = stageKey(lesson.day);
  const bonus = state.bonusLinks[bonusKey] || null;
  const bonusBlock = `
    <section class="lesson-block bonus-panel">
      <p class="eyebrow">接着看</p>
      <h3>今天学的东西，去野外见一面</h3>
      <p class="zh-note">让豆包根据你今天学的语块，推荐一个 2-3 分钟真实场景的英文短片。听完你会发现：刚才的语块在母语者嘴里就是这样跑出来的。</p>
      <div class="action-row">
        <button class="primary" id="copyBonusPromptButton">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 8h10v10H8z" /><path d="M6 16H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" /></svg>
          复制推荐提示词给豆包
        </button>
      </div>
      <label class="bonus-input">
        <span>把豆包给的视频链接粘贴回来：</span>
        <input id="bonusLinkInput" type="url" inputmode="url" placeholder="https://..." value="${escapeHtml(bonus ? bonus.url : "")}" />
      </label>
      <div class="action-row">
        <button class="secondary compact" id="saveBonusLinkButton">保存链接</button>
        ${bonus && bonus.url ? `<a class="bonus-go" href="${escapeHtml(bonus.url)}" target="_blank" rel="noopener noreferrer">打开视频 ↗</a>` : ""}
      </div>
      ${bonus && bonus.url ? `
        <div class="self-assess">
          <p class="muted">看完之后，刚学的语块在野外你听出了几个？</p>
          <div class="chip-row">
            ${["都没听出", "听出 1-2 个", "大部分听出", "全部秒懂"].map((l) => `
              <button class="accuracy-chip ${bonus.reaction === l ? "active" : ""}" data-bonus-reaction="${escapeHtml(l)}">${l}</button>
            `).join("")}
          </div>
        </div>
      ` : ""}
    </section>
  `;

  const finalBlock = `
    <section class="lesson-block active-training final-blind">
      <p class="eyebrow">Step 5</p>
      <h3>终听验证</h3>
      <p class="zh-note">现在不要看英文，再听一遍完整段落。听完选一档，对照你最开始的盲听结果。</p>
      <div class="blind-card">
        <p class="blind-title">不要打开字幕。这次的目的是感受变化。</p>
      </div>
      <div class="action-row">
        <button class="primary" data-speak="${escapeHtml(lesson.passage)}" data-rate="0.95">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
          终听正常速
        </button>
        <button class="secondary" data-record-text="${escapeHtml(lesson.passage)}" data-record-passage="1">
          <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="6" /></svg>
          按住整段录我念
        </button>
      </div>
      <div class="self-assess">
        <p class="muted">这一次你听到什么程度？</p>
        <div class="chip-row">${finalAccuracyChips}</div>
      </div>
      <p class="delta-hint">${escapeHtml(deltaHint)}</p>
    </section>

    ${bonusBlock}

    <section class="lesson-block complete-panel">
      <h3>最后复盘</h3>
      <p class="zh-note">${escapeHtml(lesson.reflection)}</p>
      <p class="zh-note">${escapeHtml(lesson.zh)}</p>
      <textarea class="reflection-box" id="reflectionBox" placeholder="用自己的中文写两三句话，确认已经听懂。">${escapeHtml(savedNote)}</textarea>
      <div class="action-row">
        <button class="primary" id="completeLessonButton">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 13 4 4L19 7" /></svg>
          ${completed ? "已完成，再保存" : "完成今日"}
        </button>
        <button class="secondary" id="nextLessonButton">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14" /><path d="m13 6 6 6-6 6" /></svg>
          下一天
        </button>
      </div>
    </section>
  `;

  const heroHints = {
    1: "今天先不看文字，只用耳朵进入这段英文。",
    2: "现在看英文字幕，把声音和英文意思接起来。",
    3: "逐句训练听到就懂，尽量跳过中文翻译。",
    4: lesson.idea,
    5: "再盲听一次，对照最开始那遍——这是今天的正反馈。"
  };

  lessonPanel.innerHTML = `
    <div class="lesson-hero">
      <p class="eyebrow">Day ${lesson.day}</p>
      <h2>${escapeHtml(lesson.title)}</h2>
      <p>${escapeHtml(heroHints[stage])}</p>
    </div>

    <div class="method-strip">
      ${stageStrip}
    </div>

    ${stage === 1 ? blindBlock : ""}
    ${stage === 2 ? subtitleBlock : ""}
    ${stage === 3 ? syncBlock : ""}
    ${stage === 4 ? shadowBlock : ""}
    ${stage === 5 ? finalBlock : ""}
  `;
}

function renderPassageWithHighlights(lesson, level) {
  let html = escapeHtml(lesson.passage);
  if (!level || level === "全懂") return html;
  const phraseList = lesson.phrases.map(([en]) => en).sort((a, b) => b.length - a.length);
  phraseList.forEach((phrase) => {
    const safe = escapeHtml(phrase);
    const escaped = safe.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const re = new RegExp(`(${escaped})`, "gi");
    html = html.replace(re, '<mark class="hl-phrase">$1</mark>');
  });
  if (level === "几个词" || level === "没听懂") {
    lesson.sentences.forEach(([en]) => {
      const safe = escapeHtml(en);
      const escaped = safe.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const re = new RegExp(`(${escaped})`);
      html = html.replace(re, '<span class="hl-sentence">$1</span>');
    });
  }
  return html;
}

function accuracyDeltaSentence(first, final) {
  const order = { "没听懂": 0, "几个词": 1, "懂大意": 2, "全懂": 3 };
  const a = order[first] ?? 0;
  const b = order[final] ?? 0;
  if (b > a) return `从"${first}"到"${final}"——这就是今天大脑长出的听感。`;
  if (b === a) return `两次都是"${first}"——继续往前走，听感的变化常常在第 3-5 课后才明显。`;
  return `回退了一档——大概率是这段语速比你目前的舒适区高半拍。明天可以多回听一次再继续。`;
}

function renderCourseList() {
  const warmupDone = isWarmupComplete();
  const trackTabs = `
    <div class="track-tabs">
      <button class="track-tab ${state.activeTrack === "warmup" ? "active" : ""}" data-switch-track="warmup">
        热身 · 7 天
      </button>
      <button class="track-tab ${state.activeTrack === "naval" ? "active" : ""}" data-switch-track="naval">
        Naval · 30 天
      </button>
    </div>
  `;

  const lockHint = !warmupDone && state.activeTrack === "naval"
    ? `<p class="lock-hint">⚠️ 建议先完成 7 天热身再来 Naval，听感会差很多。仍想试可以点开。</p>`
    : "";

  const lessons = getActiveLessons();
  const completedMap = getCompletedMap();

  const items = lessons
    .map((lesson) => {
      const done = Boolean(completedMap[String(lesson.day)]);
      return `
        <button class="lesson-card" data-open-lesson="${lesson.day}">
          <span class="day-badge">${lesson.day}</span>
          <span>
            <strong>${escapeHtml(lesson.title)}</strong>
            <span>${escapeHtml(lesson.theme)}</span>
          </span>
          <span class="status-dot ${done ? "done" : ""}" aria-label="${done ? "已完成" : "未完成"}"></span>
        </button>
      `;
    })
    .join("");

  lessonList.innerHTML = `${trackTabs}${lockHint}${items}`;
}

function renderPhrases() {
  if (!state.phrases.length) {
    phraseList.innerHTML = `<p class="empty">还没有保存语块。今天遇到值得反复听的表达，就点书签按钮。</p>`;
    return;
  }

  phraseList.innerHTML = state.phrases
    .map(
      (item, index) => {
        const primary = item.intuition || item.cn;
        const secondary = item.intuition ? item.cn : "";
        return `
        <div class="phrase-card">
          <div>
            <strong>${escapeHtml(item.en)}</strong>
            <span class="phrase-intuition">${escapeHtml(primary)}</span>
            ${secondary ? `<span class="phrase-cn-aside">${escapeHtml(secondary)}</span>` : ""}
          </div>
          <button class="phrase-save" data-remove-phrase="${index}" aria-label="删除 ${escapeHtml(item.en)}">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 7h12" /><path d="M9 7V5h6v2" /><path d="m9 11 .5 7" /><path d="m15 11-.5 7" /><path d="M8 7l1 13h6l1-13" /></svg>
          </button>
        </div>
      `;
      }
    )
    .join("");
}

function renderReview() {
  const due = getDueReviews();
  if (!due.length) {
    reviewPanel.innerHTML = `<p class="empty">今天没有到期语块。保存几个表达后，这里会自动安排复习。<br><span class="muted">间隔重复让大脑在快忘记的瞬间复盘——这是 SRS 的核心。</span></p>`;
    return;
  }

  reviewPanel.innerHTML = due
    .map((item) => {
      const record = state.reviewLog[item.en] || {};
      const mastery = deriveMastery(item.en, record);
      const isSpoken = state.spokenChunks.some((e) => (typeof e === "string" ? e : e.en) === item.en);
      const intervalLabel = record.interval ? `下次 ${record.interval} 天后` : "首次复习";
      const primary = item.intuition || item.cn;
      const secondary = item.intuition ? item.cn : "";
      return `
        <div class="review-card mastery-${mastery}">
          <div class="review-head">
            <strong>${escapeHtml(item.en)}</strong>
            <span class="mastery-tag">${mastery}</span>
          </div>
          <p class="phrase-intuition">${escapeHtml(primary)}</p>
          ${secondary ? `<p class="phrase-cn-aside">${escapeHtml(secondary)}</p>` : ""}
          <p class="muted small">${intervalLabel} · 已复习 ${record.repetitions || 0} 次</p>
          <div class="action-row">
            <button class="play-button" data-speak="${escapeHtml(item.en)}" data-rate="0.82">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
              听一遍
            </button>
            <button class="hold-button compact" data-record-text="${escapeHtml(item.en)}" data-record-index="review">
              <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="6" /></svg>
              ${isSpoken ? "再录一遍" : "按住说一遍"}
            </button>
          </div>
          <div class="grade-row">
            <button class="grade-chip grade-stuck" data-review-grade="${escapeHtml(item.en)}" data-grade="stuck">卡壳</button>
            <button class="grade-chip grade-ok" data-review-grade="${escapeHtml(item.en)}" data-grade="ok">会了</button>
            <button class="grade-chip grade-easy" data-review-grade="${escapeHtml(item.en)}" data-grade="easy">一听就懂</button>
          </div>
        </div>
      `;
    })
    .join("");
}

function getDueReviews() {
  const today = todayIso();
  return state.phrases
    .filter((item) => {
      const record = state.reviewLog[item.en];
      if (!record) return true;
      return !record.dueDate || record.dueDate <= today;
    })
    .sort((a, b) => {
      const ra = state.reviewLog[a.en];
      const rb = state.reviewLog[b.en];
      const da = ra && ra.dueDate ? ra.dueDate : "0000-00-00";
      const db = rb && rb.dueDate ? rb.dueDate : "0000-00-00";
      return da.localeCompare(db);
    })
    .slice(0, 20);
}

function deriveMastery(en, record) {
  if (state.spokenChunks.some((e) => (typeof e === "string" ? e : e.en) === en)) return "能用出来";
  if (!record || !record.repetitions) return "陌生";
  if (record.repetitions >= 3) return "一听就懂";
  return "听过";
}

function updateSrsCard(en, grade) {
  const today = todayIso();
  const prev = state.reviewLog[en] || {
    ef: 2.5,
    interval: 1,
    repetitions: 0,
    dueDate: today,
    lastReviewed: null,
    mastery: "陌生"
  };
  let { ef, interval, repetitions } = prev;
  if (grade === "stuck") {
    ef = Math.max(1.3, ef - 0.2);
    repetitions = 0;
    interval = 1;
  } else if (grade === "ok") {
    repetitions += 1;
    const ladder = [1, 3, 7, 15, 30, 60];
    interval = ladder[Math.min(repetitions, ladder.length - 1)];
  } else if (grade === "easy") {
    ef = Math.min(3.5, ef + 0.15);
    repetitions += 1;
    interval = Math.max(2, Math.round((interval || 1) * ef));
  }
  state.reviewLog[en] = {
    ef,
    interval,
    repetitions,
    dueDate: addDays(today, interval),
    lastReviewed: today,
    mastery: deriveMastery(en, { repetitions })
  };
  saveState();
  const xpMap = { stuck: 2, ok: 5, easy: 10 };
  const labelMap = { stuck: "卡壳也算练习", ok: "复习答对", easy: "一听就懂" };
  rewardAction(xpMap[grade] || 2, labelMap[grade] || "复习");
}

function refreshBackupStatusLine() {
  const el = document.getElementById("backupStatusLine");
  if (!el) return;
  const phraseCount = (state.phrases || []).length;
  const warmupDone = Object.keys(state.warmupCompleted || {}).length;
  const navalDone = Object.keys(state.completed || {}).length;
  const streak = state.streak || 0;
  el.textContent = `${phraseCount} 个语块 · 热身 ${warmupDone}/7 · Naval ${navalDone}/30 · 🔥 ${streak} 天`;
}

async function exportStateToClipboard() {
  const payload = JSON.stringify(state, null, 2);
  try {
    await navigator.clipboard.writeText(payload);
    showToast("学习数据已复制到剪贴板。建议发给自己邮箱保存。");
  } catch {
    const box = document.getElementById("importStateBox");
    if (box) {
      box.value = payload;
      box.focus();
      box.select();
      showToast("复制失败，已填到下方文本框，请手动选中复制。");
    } else {
      showToast("复制失败。");
    }
  }
}

function downloadStateAsFile() {
  const payload = JSON.stringify(state, null, 2);
  const blob = new Blob([payload], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `naval-listening-backup-${todayIso()}.json`;
  document.body.appendChild(a);
  a.click();
  setTimeout(() => {
    URL.revokeObjectURL(url);
    a.remove();
  }, 0);
  showToast("备份文件已生成，请妥善保存。");
}

function importStateFromTextarea() {
  const box = document.getElementById("importStateBox");
  if (!box) return;
  const raw = box.value.trim();
  if (!raw) {
    showToast("请先粘贴之前导出的内容。");
    return;
  }
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch (err) {
    showToast("不是有效的 JSON。请确认你粘的是完整导出文本。");
    return;
  }
  if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
    showToast("内容看起来不是导出的学习数据。");
    return;
  }
  if (!parsed.schemaVersion && !parsed.phrases && !parsed.completed && !parsed.warmupCompleted) {
    showToast("内容里没找到学习数据字段，没敢覆盖。");
    return;
  }
  if (!window.confirm("恢复会覆盖当前的进度（包括今天的）。确定继续？")) return;
  // 备份当前 state 到 localStorage 一个独立 key，万一恢复后悔
  try { localStorage.setItem("naval-listening-state-pre-import-" + todayIso(), JSON.stringify(state)); } catch {}
  state = migrateState({ ...parsed });
  saveState();
  selectedLessonDay = getActiveSelectedDay() || getFirstOpenDay();
  setActiveSelectedDay(selectedLessonDay);
  saveState();
  renderAll();
  refreshBackupStatusLine();
  box.value = "";
  showToast("学习数据已恢复。");
}

function savePhrase(en, cn, intuition) {
  const trimmedEn = en.trim();
  const trimmedCn = (cn || "").trim() || "自定义语块";
  const trimmedIn = (intuition || "").trim();
  if (!trimmedEn) return;
  if (state.phrases.some((item) => item.en.toLowerCase() === trimmedEn.toLowerCase())) {
    showToast("这个语块已经保存过了。");
    return;
  }
  state.phrases.unshift({
    en: trimmedEn,
    cn: trimmedCn,
    intuition: trimmedIn,
    createdAt: new Date().toISOString()
  });
  saveState();
  renderPhrases();
  renderReview();
  showToast("已存入语块本。");
  rewardAction(5, `保存语块 "${trimmedEn}"`);
}

function openView(viewId) {
  views.forEach((view) => view.classList.toggle("active", view.id === viewId));
  navItems.forEach((item) => item.classList.toggle("active", item.dataset.view === viewId));
  window.scrollTo({ top: 0, behavior: "smooth" });
}

function completeCurrentLesson() {
  saveLessonDrafts();
  const completedMap = getCompletedMap();
  const stages = getStagesMap();
  const alreadyCompletedToday = state.lastCompletedDate === todayIso();
  completedMap[String(selectedLessonDay)] = new Date().toISOString();
  stages[String(selectedLessonDay)] = 5;
  setActiveSelectedDay(selectedLessonDay);
  updateStreakOnComplete();
  state.trainingHistory = state.trainingHistory || [];
  state.trainingHistory.unshift({ kind: "lesson", track: state.activeTrack, day: selectedLessonDay, at: Date.now() });
  state.trainingHistory = state.trainingHistory.slice(0, 200);
  saveState();
  renderAll();
  if (!alreadyCompletedToday) {
    showHighlightDialog();
    rewardAction(30, "完整跑完一课");
  } else {
    showToast("今日已完成过一次，再保存一次进度。");
    rewardAction(5, "再保存一次");
  }
}

function updateStreakOnComplete() {
  const today = todayIso();
  if (state.lastCompletedDate === today) return;
  const yesterday = addDays(today, -1);
  if (state.lastCompletedDate === yesterday) state.streak = (state.streak || 0) + 1;
  else state.streak = 1;
  state.longestStreak = Math.max(state.longestStreak || 0, state.streak);
  state.lastCompletedDate = today;
}

function showHighlightDialog() {
  const dialog = document.getElementById("highlightDialog");
  if (!dialog) return;
  const lesson = getLesson(selectedLessonDay);
  const key = stageKey(lesson.day);
  const acc = state.blindAccuracy[key] || {};
  const order = { "没听懂": 0, "几个词": 1, "懂大意": 2, "全懂": 3 };
  const diff = (order[acc.final] ?? 0) - (order[acc.first] ?? 0);
  const arrow = diff > 0 ? "▲ +" + diff + " 档" : diff < 0 ? "▼ " + diff + " 档" : "持平";
  const dist = getMasteryDistribution();
  const spokenWeek = getSpokenChunksThisWeek();
  const streakLine = state.streak >= 7
    ? `🔥 ${state.streak} 天打卡 — 听感开始变成肌肉`
    : state.streak >= 3
    ? `🔥 连续 ${state.streak} 天 — 节奏起来了`
    : `🔥 连续 ${state.streak} 天 — 明天再回来一次，就有 streak 了`;
  const coachLine = getCoachLine(acc, dist, state.streak);
  document.getElementById("highlightContent").innerHTML = `
    <p class="hl-eyebrow">Day ${lesson.day} · ${escapeHtml(lesson.title)}</p>
    <h3 class="hl-title">今日高光</h3>
    <div class="hl-grid">
      <div class="hl-cell">
        <p class="hl-num">${escapeHtml(acc.first || "未测")}</p>
        <p class="hl-label">首遍盲听</p>
      </div>
      <div class="hl-cell hl-arrow">
        <p class="hl-num">${arrow}</p>
        <p class="hl-label">变化</p>
      </div>
      <div class="hl-cell">
        <p class="hl-num">${escapeHtml(acc.final || "未测")}</p>
        <p class="hl-label">终听</p>
      </div>
    </div>
    <div class="hl-stats">
      <div><strong>${dist["能用出来"] || 0}</strong> · 能开口语块</div>
      <div><strong>+${spokenWeek}</strong> · 本周新增</div>
      <div><strong>${(dist["听过"] || 0) + (dist["一听就懂"] || 0)}</strong> · 听过/熟</div>
    </div>
    <p class="hl-streak">${streakLine}</p>
    <p class="hl-coach">${escapeHtml(coachLine)}</p>
  `;
  state.highlightSeenForKey = key;
  saveState();
  if (typeof dialog.showModal === "function") dialog.showModal();
  else dialog.setAttribute("open", "");
}

function getCoachLine(acc, dist, streak) {
  const order = { "没听懂": 0, "几个词": 1, "懂大意": 2, "全懂": 3 };
  const first = order[acc.first] ?? -1;
  const final = order[acc.final] ?? -1;
  if (final > first && final >= 2) return "今天大脑真的在长——下一课换个语速再试一次。";
  if (final > first) return "听感在抬头。继续，第 3-5 课会有第一次明显的破壳。";
  if (final === first && final >= 2) return "稳住，听感像复利——单日看不出，7 天回头看会变。";
  if (final === first && final <= 1) return "今天像在打地基。明天重听一遍这段，再开新课。";
  if (final < first) return "回退了一档——大概率这段语速踩到了你目前舒适区上方。明天多回听 2 次再继续。";
  if (streak >= 7) return "你已经连了一周——这是 80% 人放弃的位置，你已经过了。";
  return "今天动作完整，明天继续。";
}

function goNextLesson() {
  saveLessonDrafts();
  const lessons = getActiveLessons();
  if (selectedLessonDay >= lessons[lessons.length - 1].day) {
    if (state.activeTrack === "warmup") {
      state.activeTrack = "naval";
      selectedLessonDay = state.selectedLessonDay || 1;
      showToast("热身完成，进入 30 天 Naval。");
    } else {
      selectedLessonDay = 1;
    }
  } else {
    selectedLessonDay = selectedLessonDay + 1;
  }
  setActiveSelectedDay(selectedLessonDay);
  const stages = getStagesMap();
  stages[String(selectedLessonDay)] ||= 1;
  saveState();
  renderAll();
  openView("todayView");
}

function switchTrack(track) {
  saveLessonDrafts();
  if (track !== "warmup" && track !== "naval") return;
  state.activeTrack = track;
  selectedLessonDay = getActiveSelectedDay() || getFirstOpenDay();
  setActiveSelectedDay(selectedLessonDay);
  saveState();
  renderAll();
  openView("todayView");
}

function splitSentences(text) {
  return text
    .replace(/\s+/g, " ")
    .match(/[^.!?]+[.!?]+|[^.!?]+$/g)
    ?.map((sentence) => sentence.trim())
    .filter((sentence) => sentence.split(" ").length > 5) || [];
}

function analyzeClip() {
  const title = document.getElementById("clipTitle").value.trim() || "自选英文视频";
  const text = document.getElementById("clipText").value.trim();
  if (!text) {
    showToast("先粘贴一段英文材料。");
    return;
  }

  const sentences = splitSentences(text);
  const ranked = sentences
    .map((sentence) => ({
      sentence,
      score:
        sentence.length +
        (/(leverage|startup|market|founder|invest|wealth|judgment|AI|model|risk|product|capital|happiness|status)/i.test(sentence) ? 80 : 0)
    }))
    .sort((a, b) => b.score - a.score)
    .slice(0, 5)
    .map((item) => item.sentence);
  const phrases = extractPhrases(text);

  document.getElementById("clipOutput").innerHTML = `
    <p class="eyebrow">学习卡</p>
    <h3>${escapeHtml(title)}</h3>
    <p class="zh-note">先听这些原句，再把材料交给豆包做深度拆解。</p>
    <div class="phrase-grid">
      ${phrases.map(([en, cn]) => `
        <div class="phrase-card">
          <div>
            <strong>${escapeHtml(en)}</strong>
            <span>${escapeHtml(cn)}</span>
          </div>
          <button class="phrase-save" data-save-phrase="${escapeHtml(en)}" data-meaning="${escapeHtml(cn)}" aria-label="保存 ${escapeHtml(en)}">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 4h14v16l-7-3-7 3z" /></svg>
          </button>
        </div>
      `).join("")}
    </div>
    <div class="sentence-list">
      ${ranked.map((sentence) => `
        <div class="sentence-card">
          <p class="en">${escapeHtml(sentence)}</p>
          <div class="sentence-actions">
            <button class="play-button" data-speak="${escapeHtml(sentence)}" data-rate="0.72">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
              慢速
            </button>
            <button class="play-button" data-speak="${escapeHtml(sentence)}" data-rate="0.95">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z" /></svg>
              正常
            </button>
          </div>
        </div>
      `).join("")}
    </div>
  `;
}

function extractPhrases(text) {
  const library = [
    ["specific knowledge", "特殊知识"],
    ["permissionless leverage", "无需许可的杠杆"],
    ["long-term games", "长期游戏"],
    ["status games", "地位游戏"],
    ["first principles", "第一性原理"],
    ["compound interest", "复利"],
    ["upside", "上行空间"],
    ["downside", "下行风险"],
    ["market feedback", "市场反馈"],
    ["revealed preference", "显示性偏好"],
    ["regulatory risk", "监管风险"],
    ["venture capital", "风险投资"],
    ["seed round", "种子轮融资"],
    ["product-market fit", "产品市场匹配"],
    ["founder-market fit", "创始人与市场匹配"],
    ["artificial intelligence", "人工智能"],
    ["language model", "语言模型"],
    ["capital allocation", "资本配置"],
    ["decision-making", "决策"],
    ["mental models", "思维模型"]
  ];

  const lowered = text.toLowerCase();
  const hits = library.filter(([en]) => lowered.includes(en.toLowerCase()));
  if (hits.length >= 5) return hits.slice(0, 8);

  const words = text
    .replace(/[^A-Za-z\s-]/g, " ")
    .split(/\s+/)
    .filter((word) => word.length > 6);
  const counts = new Map();
  words.forEach((word) => counts.set(word.toLowerCase(), (counts.get(word.toLowerCase()) || 0) + 1));
  const generated = [...counts.entries()]
    .sort((a, b) => b[1] - a[1])
    .slice(0, 8 - hits.length)
    .map(([word]) => [word, "视频关键词，建议交给豆包解释使用场景"]);
  return [...hits, ...generated].slice(0, 8);
}

async function copyDoubaoPrompt() {
  const text = document.getElementById("clipText").value.trim();
  const prompt = `${doubaoBasePrompt}\n\n${text || "请在这里粘贴英文字幕或英文文章片段。"}`;
  try {
    await navigator.clipboard.writeText(prompt);
    showToast("豆包提示词已复制。");
  } catch {
    showToast("复制失败，可以手动选中文字复制。");
  }
}

async function copyConversationCoachPrompt() {
  const info = getLevelInfo(state.level);
  const recentPhrases = (state.phrases || [])
    .slice(0, 10)
    .map((p) => `- ${p.en}${p.cn ? `（${p.cn}）` : ""}${p.intuition ? ` —— ${p.intuition}` : ""}`)
    .join("\n");
  const spoken = (state.spokenChunks || []).map((e) => (typeof e === "string" ? e : e.en)).slice(0, 8);
  const todayLesson = getLesson(selectedLessonDay);
  const todayPhrases = todayLesson ? todayLesson.phrases.slice(0, 6).map(([en]) => en).join("、") : "";
  const prompt = `你现在是我的私人英语口语教练。请用豆包的"语音对话"模式跟我聊——我需要的不是文本聊天，是张嘴说英语、你用嘴反馈的训练。

【关于我】
- 当前听感等级：L${state.level} · ${info.name}（${info.desc}）
- 长期目标：能听懂 CNN/BBC 新闻和英文播客对话，且能自然开口说英语，不再害怕开口
- 弱点：开口少、害怕错、肌肉记忆不够、不会用真实语块只会词典翻译
- 今天的课文聚焦语块：${todayPhrases || "（暂无）"}
- 我最近 10 个保存到语块本的高价值表达：
${recentPhrases || "（暂无，初学者，请从最简单的日常场景起）"}
- 我已经能脱口而出的语块：${spoken.length ? spoken.join("、") : "（还没有）"}

【关于语言：这一条很重要】
你的反馈和指导必须用**中英文夹杂**的方式，不要全英文也不要全中文。原则：
- 你抛给我的**场景开场、原句、native speaker 版本、要我跟读的句子**——全部用**英文**（这是我要练的输入）
- 你的**讲解、点评、修改原因、文化背景、语用提示**——用**中文**说清楚（这是我要听懂的反馈）
- 关键英文词、语块、表达——保留英文原文，紧跟一个简短的中文画面感解读（不是字典翻译，是空间/动作画面）
- 举例正确格式：「你刚说的 'I want to go'，太直白了。母语者会说 '**I'm thinking of heading out**'——thinking of 是"心里正盘算"，heading out 是"往外走"，整句的感觉是'我心里在盘算出门'。再听一遍：I'm thinking of heading out. 你跟读 2 遍。」
- 错误格式（不要这样）：全英文反馈（我听不懂为什么）；或者全中文（我没练到耳朵）

【请按这样陪我训练，每次会话都遵守】
1. **全程语音对话为主**——英文输入用嘴说，中文反馈也用嘴说，不要长篇打字
2. **难度匹配 L${state.level}**：${state.level <= 1 ? "用日常对话语速，常用词，反馈中中文占比可以多一点" : state.level <= 2 ? "正常语速，常见生活语块，反馈中英文各半" : state.level <= 3 ? "接近母语播客语速，可以用思想性词汇，反馈以中文要点为主" : "母语速度，含新闻/抽象词汇，反馈中文要点 + 英文示范"}
3. **每次会话开局，你随机抛一个具体场景**给我（点餐 / 工作汇报 / 跟朋友吐槽 / 表达不同意见 / 解释一个想法 / 谈论一篇文章）。开场 1-2 句英文，让我用英文接。
4. **我开口后，请按这个三步反馈**：
   (a) 「**你说得不错的地方**」——用中文 1 句话指出我哪里说得自然，引用我的原英文片段
   (b) 「**一个最值得改的点**」——用中文说清是发音 / 连读 / 语块选择哪一类问题，**只挑一个**，不要一次给我五个
   (c) 「**Native 版本**」——给我一句完整的英文示范，然后用中文 1 句话解释为什么这样说更地道（画面感解读），再说一遍这句英文让我跟读两遍
5. **主动出题用我学过的语块**：上下文合适时，故意把我"今天的课文聚焦语块"或"最近 10 个语块"塞进你的英文句子里，逼我听出来；每隔几轮挑一个让我必须用一次。
6. **每 5 轮挑一个我反复用错的语块**，用中文给我画面感解释（不是词典翻译，是空间/动作画面）+ 1 个真实场景英文例句。
7. **关键纪律**：你最高优先级是让我多开口、多听到我的错被中文说清楚、多听到一个 native 版本马上跟读。不要长篇大论，每一轮反馈控制在 30 秒内说完。
8. **每次结束**，用中文给我今天的 3 个 takeaway：① 你今天说得最 native 的那句（引用英文原句）② 最该回去重听的那个语块（英文 + 画面感解读）③ 下次见面前我应该录 5 遍的那句英文。

【现在开始】请给我一个 L${state.level} 难度适合的、具体的开场场景，1-2 句英文开场，然后让我用英语回。`;

  try {
    await navigator.clipboard.writeText(prompt);
    showToast("口语教练提示词已复制，去豆包打开语音对话模式粘进去。");
  } catch {
    showToast("复制失败，请手动选中文字复制。");
  }
}

async function copyDeepDivePrompt(phrase, meaning) {
  const prompt = `请用画面感+空间动作的方式，给中国 CET-4 水平学习者讲透英文短语 "${phrase}"（参考释义：${meaning || "—"}）。

请严格按这个结构输出（每条简短不啰嗦）：
1. 拆词画面：把每个词（尤其介词/小词）的物理原始意义说出来，然后让我看到合起来的空间画面
2. 一句话本质：用一句中文概括这个短语在母语者脑中的核心意象
3. 3 个真实例句：分别来自 (a) 日常对话 (b) 影视/访谈 (c) 商务/邮件，每句配一个画面
4. 推荐 1-2 个 YouTube 或 B 站短片，最好能定位到时间戳，让我直接听到母语者怎么用这个表达
5. 常见误用与注意点：中国人最容易错的一处

请避免"翻译就是 XX"的写法，多用"想象 ___ 的画面"。`;
  try {
    await navigator.clipboard.writeText(prompt);
    showToast("讲透提示词已复制，去豆包问吧。");
  } catch {
    showToast("复制失败，请手动选中文字复制。");
  }
}

async function copyBonusPrompt() {
  const lesson = getLesson(selectedLessonDay);
  const phraseList = lesson.phrases.slice(0, 6).map(([en]) => en).join("、");
  const trackLabel = state.activeTrack === "warmup"
    ? "日常生活场景（Friends / Modern Family / How I Met Your Mother 这种）"
    : "硅谷思想 / 创业访谈 / TED / Naval podcast 这种";
  const levelHint = state.activeTrack === "warmup"
    ? "语速接近真实日常，不要刻意慢"
    : "语速正常的真人对话或独白，难度匹配真实英语播客";
  const prompt = `我刚学完一节英语听力课，今天的高频语块是：${phraseList}。

请给我推荐 1 个 2-3 分钟的英文短视频片段，要求：
1. ${trackLabel}，是真实场景不是教学视频
2. 自然出现至少 2 个我刚学的语块
3. ${levelHint}，CET-4 水平踮脚能听懂
4. 给可点击的 YouTube 或 B 站链接（可带时间戳）
5. 用一句中文告诉我：为什么这个片段适合我现在的水平，以及今天哪几个语块会在里面冒出来

请只给一个最匹配的，不要给一长串选项。`;
  try {
    await navigator.clipboard.writeText(prompt);
    showToast("延伸推荐提示词已复制，去豆包问。");
  } catch {
    showToast("复制失败，请手动选中文字复制。");
  }
}

function saveBonusLink() {
  const input = document.getElementById("bonusLinkInput");
  if (!input) return;
  const url = input.value.trim();
  const key = stageKey(selectedLessonDay);
  if (!url) {
    delete state.bonusLinks[key];
    saveState();
    renderToday();
    showToast("已清掉今日延伸链接。");
    return;
  }
  state.bonusLinks[key] = { url, savedAt: new Date().toISOString(), reaction: (state.bonusLinks[key] || {}).reaction || null };
  saveState();
  renderToday();
  showToast("已保存延伸视频链接。");
}

function saveBonusReaction(reaction) {
  const key = stageKey(selectedLessonDay);
  const prev = state.bonusLinks[key];
  if (!prev || !prev.url) {
    showToast("先粘贴并保存链接。");
    return;
  }
  prev.reaction = reaction;
  prev.reactionAt = new Date().toISOString();
  saveState();
  renderToday();
}

async function copyVideoPrompt(target) {
  const videoUrl = document.getElementById("videoUrl").value.trim();
  const extraText = document.getElementById("clipText").value.trim();
  if (!videoUrl) {
    showToast("先粘贴一个视频链接。");
    return;
  }

  const prompt = videoPromptTemplate(videoUrl, extraText);
  try {
    await navigator.clipboard.writeText(prompt);
    showToast(target === "doubao" ? "视频学习提示词已复制给豆包。" : "视频学习提示词已复制给 Codex。");
  } catch {
    showToast("复制失败，可以手动选中文字复制。");
  }
}

async function startRecording(button) {
  if (mediaRecorder && mediaRecorder.state === "recording") return;
  const text = button.dataset.recordText || "";
  recordingForText = text;
  recordedChunks = [];

  try {
    recordingStream = await navigator.mediaDevices.getUserMedia({ audio: true });
  } catch (err) {
    showToast("没拿到麦克风权限。在 Safari 里允许一下。");
    return;
  }

  const mimeCandidates = ["audio/webm", "audio/mp4", "audio/aac"];
  const supported = mimeCandidates.find((m) => window.MediaRecorder && MediaRecorder.isTypeSupported && MediaRecorder.isTypeSupported(m));
  try {
    mediaRecorder = new MediaRecorder(recordingStream, supported ? { mimeType: supported } : undefined);
  } catch {
    mediaRecorder = new MediaRecorder(recordingStream);
  }
  mediaRecorder.ondataavailable = (event) => {
    if (event.data && event.data.size > 0) recordedChunks.push(event.data);
  };
  mediaRecorder.onstop = () => {
    const blob = new Blob(recordedChunks, { type: mediaRecorder.mimeType || "audio/webm" });
    if (recordingStream) {
      recordingStream.getTracks().forEach((t) => t.stop());
      recordingStream = null;
    }
    showWaveformDialog(blob, recordingForText);
  };

  activeShadowButton = button;
  button.classList.add("recording");
  button.dataset.originalText = button.innerHTML;
  button.innerHTML = `
    <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="6" fill="currentColor"/></svg>
    录音中...
  `;
  mediaRecorder.start();
}

function stopRecording() {
  if (!mediaRecorder || mediaRecorder.state !== "recording") return;
  mediaRecorder.stop();
  if (activeShadowButton) {
    activeShadowButton.classList.remove("recording");
    if (activeShadowButton.dataset.originalText) {
      activeShadowButton.innerHTML = activeShadowButton.dataset.originalText;
    }
    activeShadowButton = null;
  }
}

async function showWaveformDialog(blob, originalText) {
  const dialog = document.getElementById("recordingDialog");
  if (!dialog) return;
  const canvas = document.getElementById("waveformCanvas");
  const userAudio = document.getElementById("recordingUserAudio");
  const targetSpan = document.getElementById("recordingTargetText");
  const satisfyButton = document.getElementById("recordingSatisfyButton");

  targetSpan.textContent = originalText;
  satisfyButton.dataset.targetText = originalText;

  const userUrl = URL.createObjectURL(blob);
  if (userAudio.src) URL.revokeObjectURL(userAudio.src);
  userAudio.src = userUrl;

  let userSamples = new Float32Array(0);
  try {
    const arrayBuf = await blob.arrayBuffer();
    const ctx = new (window.AudioContext || window.webkitAudioContext)();
    const audioBuf = await ctx.decodeAudioData(arrayBuf.slice(0));
    userSamples = downsample(audioBuf.getChannelData(0), 640);
    await ctx.close();
  } catch (err) {
    console.warn("decode failed", err);
  }

  const pulseSamples = getSyllablePulses(originalText, 640);
  drawWaveformOverlay(canvas, userSamples, pulseSamples);

  if (typeof dialog.showModal === "function") dialog.showModal();
  else dialog.setAttribute("open", "");
}

function downsample(channelData, targetLength) {
  if (!channelData || channelData.length === 0) return new Float32Array(targetLength);
  const blockSize = channelData.length / targetLength;
  const out = new Float32Array(targetLength);
  for (let i = 0; i < targetLength; i++) {
    const start = Math.floor(i * blockSize);
    const end = Math.min(channelData.length, Math.floor((i + 1) * blockSize));
    let sum = 0;
    let count = 0;
    for (let j = start; j < end; j++) {
      sum += Math.abs(channelData[j]);
      count++;
    }
    out[i] = count > 0 ? sum / count : 0;
  }
  let max = 0;
  for (let i = 0; i < out.length; i++) if (out[i] > max) max = out[i];
  if (max > 0) for (let i = 0; i < out.length; i++) out[i] /= max;
  return out;
}

function countSyllables(word) {
  const w = word.toLowerCase().replace(/[^a-z]/g, "");
  if (!w) return 0;
  const matches = w.match(/[aeiouy]+/g);
  let n = matches ? matches.length : 1;
  if (w.endsWith("e") && n > 1) n -= 1;
  return Math.max(1, n);
}

const WEAK_WORDS = new Set([
  "a","an","the","of","to","in","on","at","for","and","but","or","is","are","was","were","be","been","being","do","does","did","have","has","had","i","you","he","she","it","we","they","my","your","his","her","its","our","their","that","this","these","those","as","from","with","by","into","than","then","so","just","up","out","not"
]);

function getSyllablePulses(text, length) {
  const tokens = text.replace(/[—–-]/g, " ").split(/\s+/).filter(Boolean);
  if (!tokens.length) return new Float32Array(length);

  // 1) 拆 beats —— 每个 beat 是一个音节 + 间隔
  const beats = [];
  tokens.forEach((tok, tokIdx) => {
    const clean = tok.toLowerCase().replace(/[^a-z']/g, "");
    const sylls = countSyllables(tok);
    const isWeak = WEAK_WORDS.has(clean);
    const hasEndStop = /[.!?]$/.test(tok);
    const hasComma = /[,;:]$/.test(tok);
    const isCap = /^[A-Z]/.test(tok) && tokIdx > 0;
    for (let i = 0; i < sylls; i++) {
      let stress;
      if (isWeak) stress = 0.3 + Math.random() * 0.1;
      else if (i === 0) stress = (isCap ? 0.95 : 0.85) + Math.random() * 0.08;
      else stress = 0.5 + Math.random() * 0.12;
      // 双音节词第二音节按 trochee 规律压低；三音节词中间稍重
      if (sylls === 2 && i === 1) stress *= 0.7;
      if (sylls === 3 && i === 1) stress *= 0.85;
      beats.push({ kind: "syl", stress, length: 1 + (i === 0 && !isWeak ? 0.2 : 0) });
    }
    if (hasEndStop) beats.push({ kind: "gap", length: 1.8 });
    else if (hasComma) beats.push({ kind: "gap", length: 1.0 });
    else if (tokIdx < tokens.length - 1) beats.push({ kind: "gap", length: 0.35 });
  });

  // 2) 总长度归一化到 length
  const totalUnits = beats.reduce((s, b) => s + b.length, 0);
  const unitW = length / totalUnits;

  // 3) 渲染：每个音节里画 ADSR 包络 + 多频微震荡
  const out = new Float32Array(length);
  let cursor = 0;
  beats.forEach((beat, beatIdx) => {
    const widthPx = beat.length * unitW;
    if (beat.kind === "gap") {
      cursor += widthPx;
      return;
    }
    const start = Math.floor(cursor);
    const end = Math.min(length, Math.floor(cursor + widthPx));
    const seed = (beatIdx * 17 + 3) % 31; // 给每个音节一个微变种子
    for (let idx = start; idx < end; idx++) {
      const t = (idx - start) / Math.max(1, end - start);
      // ADSR: attack 0-0.12, decay→0.7, sustain 0.7, release 0.7→0
      let env;
      if (t < 0.12) env = t / 0.12;
      else if (t < 0.28) env = 1 - 0.3 * ((t - 0.12) / 0.16);
      else if (t < 0.72) env = 0.7;
      else env = 0.7 * (1 - (t - 0.72) / 0.28);
      // 多频微震荡，模拟真实声音
      const wobble =
        Math.sin(t * Math.PI * (6 + seed * 0.4)) * 0.12 +
        Math.sin(t * Math.PI * (17 + seed * 0.3)) * 0.07 +
        Math.sin(t * Math.PI * (29 + seed)) * 0.04;
      const value = Math.max(0, env * beat.stress + wobble * env);
      out[idx] = value;
    }
    cursor += widthPx;
  });

  // 4) 整段轻度归一化
  let max = 0;
  for (let i = 0; i < out.length; i++) if (out[i] > max) max = out[i];
  if (max > 0.01) for (let i = 0; i < out.length; i++) out[i] /= max;
  return out;
}

function drawWaveformOverlay(canvas, userSamples, refSamples) {
  if (!canvas) return;
  const ctx = canvas.getContext("2d");
  const W = canvas.width;
  const H = canvas.height;
  ctx.clearRect(0, 0, W, H);

  ctx.strokeStyle = "rgba(0,0,0,0.08)";
  ctx.beginPath();
  ctx.moveTo(0, H / 2);
  ctx.lineTo(W, H / 2);
  ctx.stroke();

  const drawBars = (samples, color, side) => {
    if (!samples || !samples.length) return;
    ctx.fillStyle = color;
    const barW = W / samples.length;
    for (let i = 0; i < samples.length; i++) {
      const h = samples[i] * (H * 0.45);
      const x = i * barW;
      if (side === "up") ctx.fillRect(x, H / 2 - h, Math.max(1, barW - 0.5), h);
      else ctx.fillRect(x, H / 2, Math.max(1, barW - 0.5), h);
    }
  };

  drawBars(userSamples, "#3490dc", "up");
  drawBars(refSamples, "#f5a623", "down");

  ctx.fillStyle = "#3490dc";
  ctx.font = "12px system-ui";
  ctx.fillText("你（蓝）", 8, 14);
  ctx.fillStyle = "#f5a623";
  ctx.fillText("原文节奏（橙）", 8, H - 6);
}

function markSpoken(en) {
  if (!en) return;
  const list = state.spokenChunks;
  const exists = list.findIndex((e) => (typeof e === "string" ? e : e.en) === en);
  const isNew = exists < 0;
  if (exists >= 0) list.splice(exists, 1);
  list.unshift({ en, at: new Date().toISOString() });
  saveState();
  rewardAction(isNew ? 25 : 10, isNew ? `第一次开口 "${en}"` : `再录一遍 "${en}"`);
}

document.addEventListener("click", (event) => {
  const quickTrainBtn = event.target.closest("[data-quick-train]");
  if (quickTrainBtn) {
    startQuickSession(Number(quickTrainBtn.dataset.quickTrain));
    return;
  }

  if (event.target.closest("#conversationCoachButton") || event.target.closest("#shadowConversationCoachButton")) {
    copyConversationCoachPrompt();
    return;
  }

  const trackButton = event.target.closest("[data-switch-track]");
  if (trackButton) {
    switchTrack(trackButton.dataset.switchTrack);
    return;
  }

  const accuracyButton = event.target.closest("[data-blind-accuracy]");
  if (accuracyButton) {
    const which = accuracyButton.dataset.blindAccuracy;
    const level = accuracyButton.dataset.level;
    const key = stageKey(selectedLessonDay);
    state.blindAccuracy[key] = state.blindAccuracy[key] || {};
    const wasSet = Boolean(state.blindAccuracy[key][which]);
    state.blindAccuracy[key][which] = level;
    if (which === "first") {
      state.blindAccuracy[key].firstDate = todayIso();
    } else if (which === "final") {
      state.blindAccuracy[key].finalDate = todayIso();
    }
    saveState();
    renderToday();
    if (!wasSet) {
      const order = { "没听懂": 0, "几个词": 1, "懂大意": 2, "全懂": 3 };
      const baseXp = 8;
      const bonusForFull = level === "全懂" ? 12 : 0;
      const finalImprove = which === "final" && state.blindAccuracy[key].first
        ? Math.max(0, (order[level] || 0) - (order[state.blindAccuracy[key].first] || 0)) * 6
        : 0;
      const total = baseXp + bonusForFull + finalImprove;
      const reason = which === "first" ? "首遍盲听自评" : `终听 ${finalImprove > 0 ? "进步 +" + finalImprove + " " : ""}自评`;
      rewardAction(total, reason);
    }
    return;
  }

  const stageButton = event.target.closest("[data-set-stage]");
  if (stageButton) {
    if (stageButton.hasAttribute("disabled")) return;
    setLessonStage(selectedLessonDay, Number(stageButton.dataset.setStage));
    return;
  }

  const speakButton = event.target.closest("[data-speak]");
  if (speakButton) {
    speak(speakButton.dataset.speak, Number(speakButton.dataset.rate || 0.86));
    return;
  }

  const deepDiveButton = event.target.closest("[data-deep-dive]");
  if (deepDiveButton) {
    copyDeepDivePrompt(deepDiveButton.dataset.deepDive, deepDiveButton.dataset.meaning);
    return;
  }

  const bonusReactionButton = event.target.closest("[data-bonus-reaction]");
  if (bonusReactionButton) {
    saveBonusReaction(bonusReactionButton.dataset.bonusReaction);
    return;
  }

  if (event.target.closest("#copyBonusPromptButton")) {
    copyBonusPrompt();
    return;
  }

  if (event.target.closest("#saveBonusLinkButton")) {
    saveBonusLink();
    return;
  }

  if (event.target.closest("#highlightCloseButton")) {
    const dialog = document.getElementById("highlightDialog");
    if (dialog && dialog.close) dialog.close();
    return;
  }

  if (event.target.closest("#highlightNextLessonButton")) {
    const dialog = document.getElementById("highlightDialog");
    if (dialog && dialog.close) dialog.close();
    goNextLesson();
    return;
  }

  const phraseButton = event.target.closest("[data-save-phrase]");
  if (phraseButton) {
    savePhrase(phraseButton.dataset.savePhrase, phraseButton.dataset.meaning, phraseButton.dataset.intuition);
    return;
  }

  const removeButton = event.target.closest("[data-remove-phrase]");
  if (removeButton) {
    state.phrases.splice(Number(removeButton.dataset.removePhrase), 1);
    saveState();
    renderPhrases();
    renderReview();
    showToast("已删除。");
    return;
  }

  const gradeButton = event.target.closest("[data-review-grade]");
  if (gradeButton) {
    updateSrsCard(gradeButton.dataset.reviewGrade, gradeButton.dataset.grade);
    renderReview();
    renderDashboard();
    showToast(gradeButton.dataset.grade === "stuck" ? "明天再见。" : gradeButton.dataset.grade === "easy" ? "走远一点。" : "稳住。");
    return;
  }

  const lessonButton = event.target.closest("[data-open-lesson]");
  if (lessonButton) {
    saveLessonDrafts();
    selectedLessonDay = Number(lessonButton.dataset.openLesson);
    setActiveSelectedDay(selectedLessonDay);
    saveState();
    renderAll();
    openView("todayView");
    return;
  }

  const navButton = event.target.closest("[data-view]");
  if (navButton) {
    saveLessonDrafts();
    openView(navButton.dataset.view);
    return;
  }

  const sessionButton = event.target.closest("[data-session]");
  if (sessionButton) {
    saveLessonDrafts();
    state.sessionMinutes = Number(sessionButton.dataset.session);
    saveState();
    renderAll();
    showToast(`已切换为 ${state.sessionMinutes} 分钟节奏。`);
    return;
  }

  if (event.target.closest("#completeLessonButton")) {
    completeCurrentLesson();
    return;
  }

  if (event.target.closest("#nextLessonButton")) {
    goNextLesson();
    return;
  }

  if (event.target.closest("#recordingReplayButton")) {
    const audio = document.getElementById("recordingUserAudio");
    if (audio) audio.play();
    return;
  }

  if (event.target.closest("#recordingPlayOriginalButton")) {
    speak(document.getElementById("recordingTargetText").textContent, 0.92);
    return;
  }

  const satisfyBtn = event.target.closest("#recordingSatisfyButton");
  if (satisfyBtn) {
    const target = satisfyBtn.dataset.targetText;
    if (target) markSpoken(target);
    const dialog = document.getElementById("recordingDialog");
    if (dialog && dialog.close) dialog.close();
    renderToday();
    renderReview();
    renderDashboard();
    showToast("已计入「能开口语块」。");
    return;
  }

  const closeBtn = event.target.closest("#recordingCloseButton");
  if (closeBtn) {
    const dialog = document.getElementById("recordingDialog");
    if (dialog && dialog.close) dialog.close();
  }
});

document.addEventListener("pointerdown", (event) => {
  const holdButton = event.target.closest("[data-record-text]");
  if (!holdButton) return;
  event.preventDefault();
  startRecording(holdButton);
});

document.addEventListener("pointerup", () => {
  if (activeShadowButton) stopRecording();
});

document.addEventListener("pointercancel", () => {
  if (activeShadowButton) stopRecording();
});

document.addEventListener("pointerleave", (event) => {
  const holdButton = event.target.closest?.("[data-record-text]");
  if (holdButton && activeShadowButton) stopRecording();
});

document.getElementById("buildClipButton").addEventListener("click", analyzeClip);
document.getElementById("copyDoubaoPromptButton").addEventListener("click", copyDoubaoPrompt);
document.getElementById("copyDoubaoVideoPromptButton").addEventListener("click", () => copyVideoPrompt("doubao"));
document.getElementById("copyCodexVideoPromptButton").addEventListener("click", () => copyVideoPrompt("codex"));
document.getElementById("exportStateButton")?.addEventListener("click", exportStateToClipboard);
document.getElementById("downloadStateButton")?.addEventListener("click", downloadStateAsFile);
document.getElementById("importStateButton")?.addEventListener("click", importStateFromTextarea);

document.getElementById("phraseForm").addEventListener("submit", (event) => {
  event.preventDefault();
  const phrase = document.getElementById("customPhrase");
  const meaning = document.getElementById("customMeaning");
  savePhrase(phrase.value, meaning.value);
  phrase.value = "";
  meaning.value = "";
});

document.getElementById("installHelpButton").addEventListener("click", () => {
  document.getElementById("installDialog").showModal();
});

document.getElementById("closeInstallDialog").addEventListener("click", () => {
  document.getElementById("installDialog").close();
});

window.speechSynthesis?.addEventListener?.("voiceschanged", () => {});

if ("serviceWorker" in navigator) {
  let swRefreshing = false;
  navigator.serviceWorker.addEventListener("controllerchange", () => {
    if (swRefreshing) return;
    swRefreshing = true;
    window.location.reload();
  });
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("service-worker.js").then((reg) => {
      try { reg.update(); } catch {}
      reg.addEventListener("updatefound", () => {
        const incoming = reg.installing;
        if (!incoming) return;
        incoming.addEventListener("statechange", () => {
          if (incoming.state === "installed" && navigator.serviceWorker.controller) {
            try { incoming.postMessage({ type: "SKIP_WAITING" }); } catch {}
          }
        });
      });
    }).catch(() => {});
  });
  document.addEventListener("visibilitychange", () => {
    if (document.visibilityState === "visible") {
      navigator.serviceWorker.getRegistration().then((reg) => {
        if (reg) { try { reg.update(); } catch {} }
      });
    }
  });
}

renderAll();
